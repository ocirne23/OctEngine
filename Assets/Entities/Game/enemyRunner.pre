# Enemy Runner: fast fragile harasser - half health, NO shield (health only, like the Swarm: no
# Component Force, no bubble, no readbacks — enemy fields still shove and burn it through the
# BAKED pressure field, see enemyGrunt.pre), retargets twice as often; slips through field gaps
# on speed and picks at whatever is unguarded.
Prefab gameEnemyRunner
	Component Render
		ObjectContainer baseshapes
		Node Cube
		Type StaticMesh
		Color 1 0.25 0.5
		Scale 0.35 0.5 0.35
	Component Physics
		Body Dynamic
		Shape Sphere
		Radius 0.35
		Density 60
		Friction 0
		LockRotation true
		ContactEvents true
	Component Network
	Component GameUnit
		ShortName RUN
		Team 1
		HealthMax 30
		EnergyMax 0
		ShieldOutput 0
		MoveSpeed 7.6
		AttackInterval 0.6
		AttackDamage 2.5
		PlayerDamage 4.2
		EmitterDrain 2
