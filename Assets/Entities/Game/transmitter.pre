# Connector: a 2x6x2 m pylon (Pillar3) all long links route through — links touching a Connector
# reach farther, and one Connector carries exactly ONE medium (power, fuel or minerals).
# Spawned origin 3.0 above ground. (File keeps its historic name; the game calls it Connector.)
Prefab gameConnector
	Component Render
		ObjectContainer baseshapes
		Node Pillar3
		Type StaticMesh
		Color 0.85 0.85 0.9
		Scale 1
	Component Physics
		Body Static
		ContactEvents true
		Shape Box
		HalfExtents 1 3 1
	Component GameStructure
		MeleeRadius 1
