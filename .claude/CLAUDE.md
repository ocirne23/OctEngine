# CLAUDE.md

Guidance for Claude Code when working in this repository.

# Index

* **[Response Rules](#response-rules)**
* **[Project Description](#project-description)**
  * [Claude](#claude)
  * [Building](#building)
  * [Style](#style)
* **[Libraries](#libraries)**
  * [Core](#core)
  * [RendererVK](#renderervk)
  * [Entity](#entity)
  * [Script](#script)
  * [Physics](#physics)
  * [Particle](#particle)
  * [Force](#force)
  * [Spatial](#spatial)
  * [Nav](#nav)
  * [Profiling](#profiling)
  * [Memory](#memory)
  * [Threading](#threading)
  * [Procedural](#procedural)
  * [Network](#network)
  * [NetFuzz](#netfuzz)
  * [Audio](#audio)
  * [Animation](#animation)
  * [File](#file)
  * [UI](#ui)
  * [Input](#input)
  * [Testbed keys (InputControls)](#testbed-keys-inputcontrols)
* **[Asset files](#asset-files)**
* **[Game](#game)**

# Response Rules
* DO NOT PRAISE THE USER. If the user is correct in a statement just say "correct", nothing more.
* Always talk in ASD-STE100 Simplified Technical English.
* Always read CLAUDE.md/CONTEXT.md files.
* For each assertion, add a tag for your confidence in the statement: [Certain] for hard evidence, [Likely] for strong guess, [Guessing] for filling gaps. Rather than relying on English.
* Do not commit or push files
* Do not notify about App.exe linking failed due to in-use, the user knows to re-link if the linking failed due to in-use executable.
* Prefer to let the user test out changes rather than looking at log output/screen yourself
* Keep this file up to date

# Project Description
**OctEngine** (`C:\Github\OctEngine`). A modern C++ game engine. Windows/VS2026 only.  The `App` executable is the testbed: an ImGui editor (scene hierarchy, properties, asset browser, DSL script editor, entity editor, tweaks, profiler, memory panel) around a viewport.

## Building
* CMake, Visual Studio 18 2026 generator, build dir `Build/`
	* Reconfigure: `cmake -B Build` (only when files are added/removed — all sources are globbed; never edit CMakeLists to add files)
	* Build: `cmake --build Build --config Debug` (also `RelWithDebInfo`, `Release`)
	* Output: `Build/Code/App/<Config>/App.exe`. Run from anywhere inside the repo; `FileSystem::initialize()` walks up to find `Assets/` and makes it the working directory, so all asset paths are relative to `Assets/`
	* VS launch targets: `run-headless`, `run-server`, `run-client`, `run-netfuzz`, `run-cooked`. The `cooked` custom target configures+builds a sibling `Build-Cooked/` tree with `SCRIPTS_STATIC=ON`
	* Global compile flags: `/GT` (fiber-safe TLS — job code must not cache `thread_local` addresses across a `wait()`), `/arch:AVX2`, `/fp:fast`, `/MP`, `/GL`+`/LTCG`. `Procedural`'s diffusion .cpp TUs override to `/fp:precise /wd5050` (load-bearing — see Code/Procedural/CMakeLists.txt)
* No test suite, no linter. Verifying = it compiles + the user runs it
* Dependencies are prebuilt in `Dependencies/` (Include/Lib/Dll): Vulkan-Hpp (no exceptions, no constructors), SDL3, ImGui docking branch, Assimp, glslang/shaderc, OpenXR loader, Nsight Aftermath, box3d, Steam Audio (static; phonon(d).lib bundles pffft/mysofa/zlib), miniaudio (single header), meshoptimizer, zstd. box3d/steam-audio/meshoptimizer/zstd sources stay vendored for rebuilding the prebuilt libs — recipes in `Dependencies/CMakeLists.txt` (otherwise unused; everything links prebuilt `<name>.lib`/`<name>d.lib` pairs)

## Style
* Exclusively C++20 modules (.ixx), no headers
	* Most libraries use module partitions: `export module RendererVK:Renderer;` in the interface .ixx, `module RendererVK;` in the implementation .cpp; public surface re-exported from `Public/<Lib>.ixx`
	* `Core` uses dotted module names (`Core.Tweaks`, one module per .ixx); UI uses partitions (`UI:Scene`, `UI:ProfilerPanel`) except `UI.Gizmo`/`UI.fwd`
	* `Public/` .ixx's are importable from outside the library; `Private/` is internal
	* Std headers are re-exported as header units from `Code/Core/Public/OcSTL.ixx` (NOT Core.ixx — Core has no `import <std>` line left; it just `export import Core.OcSTL`). OcSTL owns the container backing (currently EASTL — see Core) and additionally `export import`s the std facilities that have no oc:: spelling: iostream/sstream, format, mutex/shared_mutex/condition_variable, thread/future/coroutine, chrono, execution, charconv, type_traits, atomic, new, bit, cmath, random. Add a missing std header there. DELIBERATE EXCEPTION: `<filesystem>` and `<fstream>` are NOT exported — all file/directory access goes through the File library's `FileSystem` (see File), so a library that needs the disk links File. Core itself therefore cannot do IO: `Core.Tweaks` takes injected read/write hooks (`setFileIo`, installed by main from FileSystem)
	* `Core.fwd` / `*.fwd.ixx` hold forward declarations
	* One exception: `Script/Public/ScriptAPI.h` is a plain header — the ABI shared with runtime-compiled script DLLs, `#include`d on both sides
* CONTAINERS ARE `oc::`, NEVER `std::` — `Core.OcSTL` (`Code/Core/Public/OcSTL.ixx`, `export import`ed by Core.ixx, so every importer of Core has it) is the ONE file that names the std originals; everything else says `oc::vector`/`oc::string`/`oc::span`/`oc::unique_ptr`/`oc::atomic`/`oc::move`/`oc::sort`/`oc::memory_order_relaxed`/... so swapping a backing implementation (EASTL, a hand-rolled one) is an edit THERE instead of at ~6500 call sites. Aliased: containers + adaptors, strings/views, pair/tuple/optional/variant, smart pointers, `function`, `hash`/comparators, atomics + the memory_order enumerators, and the `<algorithm>`/`<utility>` verbs the codebase uses. STILL `std::` on purpose: iostreams/sstream, chrono, mutex/lock_guard/thread/future, type_traits, `bit_cast` and the C runtime — platform surface, not container surface (formatting is the exception: use `oc::format`, never `std::format`, so the result is an `oc::string`). With the EASTL backing live, `std::vector`/`std::string` in engine code is now a hard COMPILE ERROR at any boundary that meets an oc:: type, not just a convention. Alias templates CANNOT be specialized: a `std::hash` specialization is still written `namespace std { template<> struct hash<X> ... }`, and is read back through `oc::hash`. Deliberate holdouts (leave them std::): `Script/Public/ScriptAPI.h` (plain header, no module access), the vendored `UI/Private/lib/imgui-node-editor`, and the Nsight Aftermath `namespace std` to_string blocks in GpuCrashTracker/ShaderDatabase (their calls resolve by qualified lookup into std, which a using-declaration snapshot cannot serve)
* No new/delete, only memory-safe types; raw pointers are non-owning
* Minimal comments — only where intent is genuinely hard to communicate
* Prioritize performance
* Engine singletons live in `export namespace Globals` inside the owning .ixx (`Globals::rendererVK`, `device`, `input`, `ui`, `world`, `physics`, `scriptHost`, `scriptEvents`, `assetRegistry`, `time`, `allocator`, ...). Cross-library teardown is ordered ENTIRELY by init_seg — `Code/Core/Private/InitSeg.h` (in every TU via forceinclude.h) is the single authority: `OC_SEG_*` section defines + the `OC_INIT_SEG(seg)` macro (`__pragma`, since `#pragma` doesn't macro-expand). Destruction = reverse construction; teardown order `ui → world → scriptEvents → networkManager → jobSystem → renderer globals → plain ".CRT$XCU" (audio, physics, spatialIndex, entityAllocator, input, ...)`. Every EntityPtr holder destructs before ~JobSystem (which runs shutdown(): entity destruction reaches PerWorker::local(), needing the main thread's live worker context), NetworkComponents unregister before ~NetworkManager closes the host, and world's caches release into the still-live renderer/audio. main() has NO explicit teardown calls — a new global that holds EntityPtrs or whose dtor calls another library's global must slot into InitSeg.h. Globals in *different* libraries otherwise have no defined construction order (see `ScriptEventManager::initialize` for the register-from-main pattern)
* int8/uint64-style typedefs from `Core`; glm via `Core.glm`; `assert` compiled out in non-debug via forceinclude.h

# Libraries
DEPENDENCY DIRECTION (`target_link_libraries`, PUBLIC unless noted). Everything imports Core, and a
library NEVER links one printed above it — read the stack bottom-up, each row may use every row
below it. Third-party libs in parentheses are PRIVATE (they never leak through a public interface).

```
   LIBRARY        DEPENDS ON (beyond Core)
   ------------------------------------------------------------------------------------------------
   App            everything + Game + AppScripts
   Game           Entity, Force, Physics, Input
   Input          UI                                             (+ openxr_loader)
   UI             Entity, RendererVK, Script                     (+ imgui)
   Entity         RendererVK, File, Script, Physics, Audio, Particle, Force, Spatial, Threading, Network, Nav
   ------------------------------------------------------------------------------------------------
   Particle       RendererVK, File
   Force          RendererVK
   Procedural     RendererVK, File, Spatial, Physics             (+ onnxruntime, zstd)
   RendererVK     Animation, File, Threading                     (+ vulkan, glslang, Aftermath)
   ------------------------------------------------------------------------------------------------
   Script         File          (ScriptHost compiles/loads .dsl from disk; disk access is File-only)
   Spatial        Threading
   Nav            Threading     (flow fields — knows nothing about entities; Game feeds it, units read it)
   Physics        Threading     (box3d's solver fork/join runs as jobs)   (+ box3d)
   File           Animation                                      (+ assimp, zlib, meshoptimizer)
   ------------------------------------------------------------------------------------------------
   Animation      -                                              -- these four are Core-only
   Network        -                                              (+ Ws2_32, Bcrypt)
   Audio          -                                              (+ Steam Audio, miniaudio)
   Threading      -
   ------------------------------------------------------------------------------------------------
   Core           std header units, oc:: containers (OcSTL), math, Profiler + MemoryTracker  -- none
```

## Core
* Cross-library utilities; wrappers for big headers (glm, SDL, imgui, Windows — the Vulkan wrapper is RendererVK-private `Util/VK.ixx`)
* `Allocator` (global new/delete routed through it), `Log` (ring buffer the UI Log panel snapshots), `Time`/`Timer`, math types (`Transform`, `Frustum`, `AABB`, `Sphere`, `Rect`, `Camera` — `Camera::screenToWorld` for viewport picking)
* `Core.OcSTL` (`OcSTL.ixx`): THE backing seam — it owns every std/EASTL header unit the engine imports and hands out the `oc::` vocabulary every other file spells. ALIASES ONLY, one line per type. Its only imports are header units, so it sits BELOW everything including `Core.Profiler` (which imports it instead of Core, since Core re-exports Profiler). **THE BACKING IS EASTL** — `#define EASTL` at the top of the file, translated at once to an internal `OC_EASTL` and `#undef`'d, because `EASTL` is also the include-DIRECTORY name and an `import <EASTL/x.h>;` macro-expands its header name (a `#include` does not). Comment that one line out for the std containers. EASTL arrives as HEADER UNITS, never textual GMF includes: a GMF include leaves its free operators (`string ==`, `+`, container comparisons) unreachable to every importer of Core, since only the class templates are decl-reachable from the aliases. The vocabulary is written against a `namespace ocstl` alias (`= eastl` / `= std`), so a name both libraries have needs no `#ifdef`. What genuinely differs: `oc::allocator<T>` (EASTL allocators are not element-typed — the parameter is ignored, and `oc::allocateArray/deallocateArray` absorb the element-vs-byte difference for Core.SmallVector), `oc::dynamic_extent` (EASTL's is `static`, so a header unit does not export it), and `initializer_list`/`char_traits`/ALL ATOMICS which are std in BOTH backings — an atomic allocates nothing and EASTL's is a strict subset (no `atomic_ref`, no C++20 wait/notify which the JobSystem eventcount and JobMutex are built on, and its memory orders are TAG TYPES rather than one enum). `allocator_traits`/`basic_string`/`basic_string_view` are absent: EASTL cannot express them at the same arity. `oc::make_unique<T[]>` is spelled out rather than aliased: EASTL's is `new T[n]` (DEFAULT-init) where std's is `new T[n]()` (VALUE-init) — a silent divergence that shipped garbage into every zero-assuming array (`BitRangeAllocator`'s grown tail read as fully allocated and asserted at startup; script data, profiler records and the char buffers assume zeroing too). A vocabulary entry must mean the same thing under either backing. FILLING THE C++20 GAPS EASTL HAS: `oc::contains` (no member `.contains()`), `oc::startsWith`/`oc::endsWith`, `oc::getline`, `oc::format` (returns an `oc::string`; ALL formatting goes through it, never `std::format`), `oc::as_bytes`, plus in-module shims — `operator<<` for eastl strings, and `std::formatter` specializations so an `oc::string` is a valid `std::format` argument
* CROSSING TO std:: — `oc::toStd` / `oc::fromStd` (strings and vectors) is the sanctioned conversion, and a no-op copy under the std backing so call sites stay backing-agnostic. Needed at every third-party boundary, all marked in place: Vulkan-Hpp `enumerate*` (returns std::vector), `vk::ClearColorValue` (spelled in `std::array`) and `vk::ResultValue`'s tuple conversion (`std::tie`), glslang `preprocess`/`GlslangToSpv`/`OutputSpvBin` (std::string + std::vector<unsigned> by reference), ONNX `Session::Run` (`std::vector<Ort::Value>` — move-only, so it stays std entirely), `<filesystem>` inside File (SceneCooker funnels through one local `toPath`), the iostream/sstream parsers, and the Nsight Aftermath `to_string` overloads
* Core links `EASTL$<$<CONFIG:Debug>:d>.lib`, and the engine supplies the three hooks EASTL declares but expects the APPLICATION to define: its two named/aligned `operator new[]` forms live in `Core.Allocator` next to the engine's other global `operator new` definitions (so EASTL allocations land in `Globals::allocator` and the MemoryTracker), and `EA::StdC::Vsnprintf` — the one that is not an allocation — sits in `Core/Private/Core.cpp`. The ALIGNED form deliberately does not over-align: `eastl::allocator::deallocate` frees every block with plain `delete[]`, so an `_aligned_malloc` block (what the `align_val_t` overloads return) would reach `Allocator::deallocate` and corrupt the heap. It asserts instead; EASTL only calls it for over-aligned element types, so nothing reaches it today
* `Core.SmallVector` (`SmallVector.ixx`): the engine's own contiguous containers, `import Core.OcSTL` (never the reverse) and re-exported by Core.ixx alongside it. `oc::small_vector<T, N>` keeps the first N elements INSIDE the object and spills to the heap past N — the per-frame scratch list that rarely exceeds N does zero allocations; `oc::fixed_vector<T, N>` is `small_vector<T, N, false>`, which refuses to spill (asserts and drops, so a release build never writes out of bounds)
* Containers: `LPMultiMap`, `LockFreeList`, `ExpiryCache`, `BitRangeAllocator`, `CheckedPtr`/`RefCheckable` (debug-checked non-owning pointers)
* `Core.VrSession`: `IVrSession` interface (opaque handles, no OpenXR types) — implemented by RendererVK's `OpenXRSession`, consumed by Input's `VrInput`, so Input and RendererVK stay unlinked
* `Core.Tweaks`: `Tweak::floatVar/intVar/boolean/color3/...("Category/Sub", "Name", &liveVariable, ...)` once at init exposes a variable in the TweakPanel. Pointers are non-owning; variable must outlive registration. This is the standard way to make anything runtime-configurable. OPTIONAL `ETweakFlags` (last param on every helper, after onChange; or `Tweak::ScopedFlags` RAII to flag a whole registerTweaks block — explicit per-call flags win): `Saved` persists to `Assets/Local/tweaks.cfg` (main() calls `TweakRegistry::loadSaved()` right after FileSystem::initialize; per-frame `TweakRegistry::update(dt)` in the main loop poll-detects changes — the panel writes through raw pointers, so polling is the only hook — and debounce-saves 0.5 s after the last change + at exit; unknown file keys are preserved for other run modes); `Synced` broadcasts server→clients (NetworkManager: full `packSynced` chunk set to a joiner after the spawn replay, re-broadcast when `syncGeneration()` bumps, over the engine-reserved event "OcTweakSync" intercepted in fireEventAttributed — never reaches scripts/game hooks, only CLIENTS apply, and `applySyncedBlob` ignores keys the receiver didn't flag Synced + clamps to the receiver's own bounds). Identity is "Category/Name" — renaming orphans the saved value. All Game/* tweaks are Saved|Synced except Game/Camera (Saved only — personal preference)

## RendererVK
* Modern Vulkan renderer (minimum spec RTX 2000); Device Generated Commands for everything; reversed-Z main view (near=1/far=0; shadow maps stay standard; fullscreen passes disable depth test)
* Lights culled via a world-space hash table into distance-scaled grids; GI via scrolling clipmap radiance cascades (ray-traced probes); RTAO, TAA, PCSS cascades, volumetric fog, GPU compute skinning. An "RT sun shadow" toggle replaces the PCSS cascades entirely (shadow cull+draw skipped)
* Long-range sun shadows ("Shadows/Terrain march *"): past the PCSS `Max distance` and `RT/TLAS Range`, distant pixels cone-march the baked terrain height cascades (`terrainSunVisibility` in `terrain_height.inc.glsl`) and take `min` with the shadow term; the march fades in inside cascade range so the handover is seamless. Steps double from `Terrain march bias` (also the self-shadow bias). Deterministic — no jitter or temporal integration
* "TAA/Enabled" off BYPASSES the pass completely — the secondary CB is not recorded, the dispatch and its GPU scope are skipped, and eye adaptation + composite sample this frame's SceneColor directly (`resolvedLayout` SHADER_READ_ONLY instead of TAA's GENERAL storage image; the scene-colour barrier gains the fragment stage since the composite then reads it). It used to only zero the feedback, so a disabled TAA still ran a full-screen resolve that copied — a "TAA" scope in the profiler for a pass doing nothing. The tweak carries `onReRecord`, so toggling rebuilds the descriptors either way
* Everything upstream of composite is linear HDR: `EyeAdaptationPipeline` (log-luminance histogram auto-exposure) feeds `CompositePipeline` (HDR→display into the swapchain before ImGui; tonemap off/Reinhard/ACES/AgX)
* FRAME SLOT WAIT: `Renderer::waitFrameSlot()` blocks on the current slot's fence and is called FIRST THING in the main loop — before the "main loop" profiler scope opens and before input is sampled — so the vsync/GPU stall shows as its own top-level "Fence wait" (the main-loop scope measures only real work) and input → sim → present stays tight (the wait used to sit inside beginFrame, between input sampling and the sim: ~4 ms of stale input at 165 Hz). Nothing may write a slot's host-visible per-frame buffers before it (renderNode instance memcpys, LOD redirects, sparse transforms — torn slot data was the one-frame LOD-flash bug); `beginFrame` asserts it ran (and waits anyway), `present()` re-arms it when the slot advances. Idempotent within a frame
* Frame order (`recordCommandBuffers()`, called from `present()`): skinning → ocean sim → indirect cull → light grid → force compute → particle sim → shadow cull+draw (unless RT sun shadow) → G-buffer → GI probes → RTAO → volumetric fog → scene forward → TAA → eye adaptation → composite + ImGui. Inside scene color: static mesh forward → decals → GI-probe debug → debug lines → force field → particles → fog apply
* Layout under `Private/`:
	* `Objects/` — thin Vulkan wrappers (Device, SwapChain, Buffer, ComputePipeline/GraphicsPipeline, AccelerationStructure, GBuffer, SceneColor, ShadowMap, GpuProfiler, BakedWorldMap, ...)
	* `Pipeline/` — one class per pass/feature (StaticMeshGraphics, GBuffer, GIProbe, RTAO, TAA, VolumetricFog, EyeAdaptation, Composite, Skinning, DebugLine, Particle, Decal, ForceField, OceanSimulation, light grid + indirect/shadow cull compute, shadows). Each registers its own tweaks
	* `Data/` — MeshDataManager, TextureManager, TextureStreamer, MeshStreamer, StagingManager, ShaderDatabase, GpuCrashTracker (Aftermath)
	* `Renderer.ixx/.cpp` — orchestrates everything; per-pass `record*()` methods
	* `OpenXRSession.ixx` — VR (`Globals::openXR`, implements `IVrSession`). Two-phase init around Vulkan device creation; stereo = 2-layer multiview swapchain; in VR the editor viewport sub-rect is ignored. `Renderer::beginFrame(camera, viewportRect)` applies the rect (`setViewportRect` is private — rect changes only at frame boundaries; beginFrame builds the projection from its aspect)
* Scene objects: `ObjectContainer` wraps one loaded `ISceneData`; `spawnNodeForIdx()` returns a `RenderNode` (RAII, move-only), `spawnSkinnedNode()` the GPU-skinned variant (AnimatorComponent feeds the bone palette via `allocateSkinningPalette`/`setSkinningPalette`). Destroying a RenderNode recycles without GPU sync (transform slots free-listed, skinned bundles parked in place per container, rebased sub-node offsets shared). `~ObjectContainer` frees ALL renderer resources (`Renderer::removeObjectContainer`)
* Per-frame instance flow: `renderNode(node, passMask)` is LOCK-FREE (callable from any job between beginFrame/present; atomic cursor claims, monotonic — on overflow the tail drops one frame, `m_instanceOverflowStart` CAS-min records it, present() clamps, capacity regrows next beginFrame; NEVER roll a bump cursor back). `addLightInfo`/`addFogVolume`/`addDecal` are lock-free too; `addDebugLine` + transform dirty lists are `PerWorker`-staged, merged in present(). Pass masks `PASS_MAIN/SHADOW/GI` (Layout.ixx = Spatial's bits); main cull, shadow cull, and GI TLAS writer each early-out on their bit (TLAS also range-bounds by `RT/TLAS Range`). Transforms upload SPARSELY: write only via `RenderNode::setTransform` (change-detected, per-frame-in-flight dirty lists) — a mutable getTransform would bypass tracking
* Texture mip streaming (`TextureStreamer`, "Texture Streaming" tweaks): DDS textures load tail-only (mips ≤128px) and stream against a VRAM budget by camera priority. Residency changes recreate the image and rewrite the slot in all 7 bindless texture arrays (UPDATE_AFTER_BIND, applied per frame slot after the fence wait); disk reads on a worker thread; old images retire after NUM_FRAMES_IN_FLIGHT. Non-DDS/procedural/embedded textures are pinned at full res
* Mesh data streaming (`MeshStreamer`, "Mesh Streaming" tweaks): cooked scenes register mesh sets (source mesh + LOD levels + .vsc byte ranges); vertex/index mega-buffers are BitRangeAllocator free lists (`MeshDataManager`). Over "Mesh budget (MB)", least-recently-referenced sets unseen for "Mesh cold frames" evict: MeshInfos get indexCount 0 (DGC/shadow/TLAS become no-ops, no re-record; static BLASes persist as geometry snapshots), ranges free after NUM_FRAMES_IN_FLIGHT. Re-reference re-streams from the .vsc on a worker; the LOD selector renders the nearest resident level meanwhile. A LOD chain shares ONE BLAS at "RT/BLAS LOD level" (alias table; aliased RT meshIdx rides the TLAS instance sbtOffset for hit-shader fetches, materials stay per-instance); static BLASes copy-compact after build ("RT/BLAS compaction", ~30-50% back). Only cooked static meshes stream. With Spatial culling everything visible stays referenced, so only despawned containers and Col_ proxies go cold
* Mesh LODs ("LOD" tweaks): `LodN_<name>` meshes/nodes form authored chains (level>0 never instanced, like `Col_`); static meshes without authored LODs get meshopt-generated index-only chains (pre-baked by the scene cache). Selection is per-instance ON THE GPU (`mesh_lod.inc.glsl` in main + shadow cull; params live via UBO): instances push LOD0 and the cull redirects the draw — CPU sizes every chain member's bucket to the chain's full count, hysteresis lives in per-instance GPU slots (benign races), shadow cull selects stateless 4x/+2 coarser, GI needs no selection (one chain BLAS). Selection metric is screen-space error (meshopt simplify errors projected to pixels vs "Max error (px)"); authored chains without error data use projected diameter vs "Full-res pixels (Authored Lods)". renderNode keeps every level of a referenced chain warm; the cull falls back to the nearest resident level during re-streams. Skinned meshes get bind-pose-generated chains too (per-instance MeshLodGroups, recycled with the bundle); skinned output MeshInfos are excluded from static BLAS builds/compaction
* Record command buffers once (not per frame) when possible; call `setHaveToRecordCommandBuffers()` after invalidating changes
* Shaders are GLSL in `Assets/Shaders/`, compiled at runtime with glslang — shader edits need no rebuild; F5 calls `reloadShaders()`. `*.inc.glsl` are includes; `shared.inc.glsl`/`ubo.inc.glsl` structs must stay in sync with `Private/Layout.ixx` (`RendererVKLayout`). SPIR-V + source dumps land in `Assets/Local/` for Aftermath crash analysis

## Entity
* The ECS. An `Entity` (pos/scale/rot, parent, interned name, spawn template, refcount, `typeBits`, `flags`) and its components live in ONE allocation. `typeBits` masks the 13 component types (fixed order: Scene, Render, Animator, Physics, Audio, Particle, Force, Light, Network, GameUnit, GameStructure, GameProjectile, Script — Script last; `EComponentID` in `EntityP.ixx`, `MaxInlineComponentTypes` 13); `getComponent<T>`/`hasComponent<T>` compute byte offsets from compile-time sizes (`Component.ixx`). Each component is a partition under `Private/Components/` (struct + `get*SpawnInfo`/`write*SpawnInfo` in .ixx, bodies in same-named .cpp); `Component.ixx` re-exports them and holds the shared machinery. Per-component .ixx's must NOT import `:Component` (cycle) — cross-component code goes in the .cpp implementation units. Adding a component type: new .ixx/.cpp pair with a `getId()` struct, `export import` from Component.ixx, entries in `EComponentID`/`inlineSizes`/`componentTypeName`, create/destroy switches in Entity.cpp, parse branch in `World::buildTemplate`, write branch in Prefab.cpp, require-slot fill branch in `ScriptComponent::syncScriptData` (a missing branch shifts every later slot — this crashed once), Entity.natvis offset entry, bump `MaxInlineComponentTypes`, and an Entity Editor section if authorable
* `EntityPtr`: intrusive refcounted handle (atomic). `Entity::create(template, transform)` placement-news into `Globals::entityAllocator` (lock-free: atomic chunk bump + tagged per-size-class Treiber free lists; blocks ≤64KB). Tree via `SceneComponent::children` + `Entity::parent`; `reparentEntity` moves ownership
* Tree spawns are ONE allocator block (template's cached `treeAllocSize`, bump-carved). While intact (`EEntityFlag_RootAllocation`/`ContiguousAllocation`) the root frees the whole block (refcount pre-walk `contiguousTreeSolelyOwned`); reparenting/deleting a member out of its birth tree SPLITS the allocation (`breakContiguousAllocation`: path ancestors demote to per-entity freeing, off-path subtrees promote to their own RootAllocations). Moving within the same allocation or grafting a whole allocation root never breaks anything
* `Entity::update` is a thin serial recursive wrapper around `Entity::updateSelf`, the ONE hot per-entity visit: Script → Network → Animator → Physics, compose world transform, refresh the SpatialIndex entry, push the RenderNode (gated on `spatialIndex.getPassMask` when Spatial culling mode ≥ Cull)
* Entity state lives in `flags` (`EEntityFlags`): `EEntityFlag_Enabled` (off prunes the subtree from update and suspends physics bodies; serialized as entity-level `Enabled false`, default true), `EEntityFlag_Frozen` (script Update/animator/physics don't tick, script events don't fire — what the Entity Editor sets on an open document). Frozen is a SUBTREE state written down the whole tree by `setFrozen`; `Entity::create` inherits it from the spawn parent, `reparentEntity` adopts the new parent's state — every check is a plain self `isFrozen()`, including entry points outside update (global script events, physics contacts). Script OnSpawn runs even frozen. `EEntityFlag_PhysicsSuspended` latches the disable walk (cleared up the new ancestor chain by `reparentEntity` — else a body joining a disabled subtree stays live and collides invisibly). Per-component `enabled` bools are independent of both
* PARALLEL entity update (`World::update(renderer, dt)`, always on): the tree is walked BREADTH-FIRST BY DEPTH, whole level concurrently — in COST-BUDGETED batches, not a uniform grain: every entity carries `updateCost` (u8 in 250ns units, MEASURED: 0 = unmeasured, the entity's FIRST update is timed and becomes the cost — no guessed initial value — then a ~1/1024-per-frame random re-measure, salted by `m_updateFrame` + the entity pointer, keeps it honest; `sizeof(Entity) == 64` static_asserted, one cache line), a batch fills until the summed cost reaches the budget = min(~25 µs worth, level total / (contexts × 4) — the over-partitioned even split keeps every worker fed on cheap levels and leaves the shared cursor slack to absorb long batches; unmeasured entities count as 1), and the level parallelFors over batches; `m_updateCost`'s EMA is fed cost units as its item count, so the budget self-calibrates to real ns-per-cost-unit. `updateSelf` emits children as `EntityUpdateNode{entity, parentWorld}` into `PerWorker` staging; the level barrier merges slots into the next level. The barrier IS the ordering guarantee — a parent always finishes before its children, so a child reads its parent only through `parentWorld` and all other writes land in the entity's own components. Thread-safe inline: scripts (DSL cross-entity access is parent→child writes / child→parent reads; world radius queries are the known open hole), animator + `setSkinningPalette` (disjoint regions), dynamic-body pose reads, `RenderNode::setTransform`/`renderNode`, `SpatialIndex::updateEntry`, audio follow, particle/force emitter slots. The pass NEVER writes to box3d. The gizmo entity uses the serial path
* `World` (`Globals::world`): `spawn("prefabName", transform)`, `spawnAssetFile("Entities/x.pre", ...)`, `createEmptyEntity`. Builds and caches `EntitySpawnTemplate`s (archetype + per-component `SpawnInfo` recipes); caches ObjectContainers, collision sources/meshes, retargeted clip sets — each source file imported once. `reloadPrefabs`/`invalidatePrefab` for editing (retired templates stay alive for live entities). OWNS the root entity list (`addRootEntity`/`removeRootEntity`/`rootEntities` const-only/`clearRootEntities`; roots die in ~World, ordered by the init_seg teardown scheme — see Style). Applies queued `EntityChange`s via `handleEntityChange(change, camera, viewportRect)`; UI notifications route back through `setOnPrefabOpened`/`setOnEntityRespawned` callbacks. `World::handleContactEvent` is the physics contact callback (passed to `physics.update` from main): invokes `PhysicsComponent::onContact` + the script's `OnPhysicsEvent`
* `AssetRegistry` (`Globals::assetRegistry`): `scanDirectory()` (cwd = Assets/) registers ObjectContainers (.oc), clips (.anm), animator graphs (.apl), prefabs (.pre) by name
* Prefabs: `savePrefab(root, path)` serializes via the per-component `write*SpawnInfo`s (the spawn recipe IS the serialization); spawned prefabs are locked instances (`EEntityFlag_PrefabInstance`, unpackable in the Scene panel); `prefabWouldCycle` guards recursion
* Per-entity TINT: `Component Render` takes `Color r g b` — RenderComponent::spawn overrides every mesh instance's material via `RenderNode::setMaterialOverride` + `Renderer::createSolidColorMaterial(color)` (cached per RGB8: a 1x1 texture + MaterialInfo minted once per color; instances carry materialIdx per record, so no shader/layout changes). All Game prefabs author unique colors this way
* Components: `SceneComponent` (children), `RenderComponent` (RenderNode + local transform, static or skinned), `AnimatorComponent` (AnimationPlayer + AnimStateMachine from .apl; gameplay via `stateMachine.setFloat/Bool/Trigger`, clip events via `onEvent`), `ScriptComponent`, `PhysicsComponent`, `AudioComponent`, `ParticleComponent`, `ForceComponent`, `LightComponent`, `NetworkComponent`, plus the GAME components (ID 9/10/11, `Components/GameComponents.ixx` — one partition for all three): `GameUnitComponent` (team/health/shield battery + C++ steering/targeting/melee/spitter stance; DSL sets orders via `self.unit.setTarget`), `GameStructureComponent` (team/health/blueprint/invulnerable/meleeRadius + own ForceQuery territory damage + atomic `damage()`/`addLoad()` intake), `GameProjectileComponent` (team-tagged lifetime/deflection/contact damage routed from `World::handleContactEvent`). CONTRACT: `update()` simulates only when NOT a network client (mirrors write state on clients); cross-entity writes are atomic CAS (`damage`/`addLoad`), cross-entity lookup is spatial queries — no entity lists; physics writes ride the body-command queue (parallel pass). Tuning statics (`GameUnitComponent::params` etc.) are set by the Game layer's tweaks. DSL surface: `self.unit.*`/`self.structure.*`/`self.projectile.*` + `ctx->gameUnit*/gameStructure*/gameProjectile*` thunks. Authored in .pre as `Component GameUnit` (Team/HealthMax/EnergyMax/ShieldOutput/MoveSpeed/Accel/AttackRange/AttackDps/PlayerDps/EmitterDrain/Ranged/StandoffRange/FireInterval), `Component GameStructure` (Team/HealthMax/Invulnerable/MeleeRadius), `Component GameProjectile` (Team/UnitDamage/StructureDamage/Lifetime/EmitterDrain/EmitterDrainRadius); all three skipped headless. ADOPTED by the Game layer: unit/projectile/structure prefabs author them, per-type unit stats live in the .pre files (Component GameUnit blocks), and Code/Game holds NO entity lists — NpcSystem is production+servicing over one arena spatial query per frame, and StructureSystem has NO roster either: `GameStructureComponent` additionally carries the stable structureId, the three stores/capacities/bands and the LINKS (`GameStructureLink` mirrored on both endpoints; each link is processed EXACTLY ONCE per tick by its OWNER side — pushing when it is the source, pulling when the far side is — in the entity pass with atomic reserve/add/return transfers. Both endpoints processing (each deciding independently) moved every link twice per frame in opposite directions, which made balancing links slosh visibly. The owner gathers its OUTGOING links first and splits the store as a per-medium FAIR SHARE across receivers — a full receiver's share flows to the rest; pulls are unbudgeted (the far side's links belong to their own owners) but atomically clamped. Fairness runs on the RECEIVING end too: a push is capped at the destination's headroom DIVIDED by the links that can feed it, so a full-but-draining destination trickles in from all its feeders steadily instead of handing each tick's scraps to whichever worker ran first (which made the inflow hop between links); `link/unlink/unlinkAll` are the main-thread bookkeeping — a structure is ALWAYS unlinked before destruction so links never dangle) plus machine state: `powered`/`flowUtil` are common, and the TYPE-SPECIFIC rest lives in a UNION discriminated by the game's structure type (`barracks` spawnTimer/boost/aliveUnits/route, `turret` fireTimer, `emitter` outputFrac/outputFracTarget/down/unitLoad — the NetEntityState pattern: only the prefab's variant is ever touched, `strainable` stays common because units probe it on arbitrary structures). NO id-keyed maps anywhere — cooldowns/tallies/boosts die with their structure. StructureSystem works on `m_frame`, a per-frame spatial-query view (`refresh()` at the top of tickAuthority/tickMirror; type derived from the entity NAME, extractor→node from position; `stampTuning` re-stamps capacities/bands/throughputs each frame so tweaks stay live) and keeps only placement/requests/production/sweeps/mirrors/save-load
* `AudioComponent`: named triggerable sounds — `Component Audio` with `Sound <alias>` entries, each one or more `Path` clips with per-Path settings (`Volume`/`Pitch`/`Loop`/`Relative`/`ReferenceDistance`/`MaxDistance`/`Rolloff`) + optional `Select` mode `Single`/`Random`/`RandomNoRepeat`/`Cycle`/`CycleStartRandom`. `trigger(entity, alias, overrides)`; playing spatial sounds follow the entity unless position-pinned. Demo: physicsCube.pre
* `LightComponent` (ID 7): a LIST of lights (`Component Light` with `Light Point/Spot/Area/Tube` children: `Color`/`Intensity`/`Range` + entity-space `Offset`/`Direction`, then `ConeAngle`/`EdgeSoftness` (Spot), `Width`/`Height`/`Rotation` (Area), `Radius`/`Length` (Tube), `Enabled`; component `Debug true` + "Lights/Debug geometry" tweak draw wireframes. Demo: Entities/Debug/lightRig.pre). Lights are PER-FRAME RECORDS: `update` calls `renderer.addLightInfo` per enabled light (lock-free, rides the parallel pass); spawn/destroy own nothing. Not frozen-gated (placement, not simulation); range/size are WORLD units, not scaled by the entity. `Direction` always means where the light POINTS — the component reconciles the renderer's encoding (`LightFrame` in LightComponent.cpp; area lights encode height-axis + roll, `TubeLight` stores FULL length and the renderer halves — do not "fix" either side alone). Script surface: a `Light` DSL struct (mirrors `OcLight` in ScriptAPI.h) + `self.light.lights` writable sequence (`foreach [ref] Light l in ...` / `ifexist ... at i`, `self.light.count`); ABI is only `entityGetLightComponent`/`lightGetCount`/`lightGetAt`/`lightSetAt` (`lightSetAt` clamps/normalizes host-side). NodeEditor Get/Set Light nodes and the Entity Editor Light section go through the same surface
* Script glue lives here, not in Script: `Globals::scriptContext` (ABI singleton + host thunks) and `ScriptEventManager` (`Globals::scriptEvents`) — global named events (`fireEvent("W Down")`), scripts subscribe via On Event entries, mapping rebuilt on reload. Its `initialize()` must run from main before any scripted entity spawns. Deferred script-driven mutations (destroy/reparent/spawn requests) are a mutex-guarded `EntityChange` queue in ScriptEventManager, drained by the main loop
* THUNK THREAD-SAFETY (scripts tick on workers): `ctx->spawnEntity` queues `EntityChange::SpawnAtPosition` and returns null (the DSL types `world.spawn` as `Entity?`, so scripts take the miss branch on the spawning frame); `internString` is shared_mutex; script RNG is thread_local; `spatialQueryRadius`'s buffer is thread_local. The DSL array registry is CHUNKED (`g_arrayChunks`, 64×1024, handle = 16 bits) so storage never moves and resolve is lock-free

### Multiplayer
* `Entity:NetworkManager` (`Globals::networkManager`) + `NetworkComponent` (ID 8). The inline component is identity only (netId + ownerClientId + `unique_ptr<NetEntityState>`, 16 bytes); all sync state lives in `NetEntityState` (NetworkComponent.ixx), allocated only inside a session. Role-exclusive state is a UNION of `ServerState`/`ClientState` (a process is one role for life; both trivially destructible); only `input`/`transferredOwnership`/`sleepDirty` are dual-role. Cross-role reads are bugs. The manager never null-checks `state`; the component's own update() and outside readers (EntityEditor, InputControls) must
* One App.exe for all roles: `--server [--port N] [--headless] [--tickrate N]` / `--connect <ip[:port]>` / `--no-encrypt` / no flags = single player (`ENetRole::None`, everything inert; `registerTweaks()` still runs). Hard cap `MaxClients` 32
* Thread contract: manager writes on the main thread pre/post the parallel pass (`receive()` before, `send()` after); `NetworkComponent::update` reads/writes only its own entity's block on workers. Client physics corrections go through the thread-safe body-command queue (`teleportBody`/`queueBodyCommand`) — never direct box3d writes from workers
* HEADLESS SERVER (`headlessServer` branching inside main() — one init sequence and ONE main loop for all modes): no window/renderer/UI/input. `World::setHeadless(true)` BEFORE any spawn makes `buildTemplate` emit only Scene/Physics/Script/Network components (component thunks do NOT null-check their handle — `//@@require`'s live `requirementsMet` gate is the guarantee a script never reaches an absent component, so a script using a stripped component simply never runs its entry points headless; cross-entity thunks still `getComponent`-check; `Renderer::isInitialized()` gates the few thunks that touch renderer state). Hull/Mesh colliders still build (container name parsed textually, renderer-free import). Loop: entity-change drains → receive → scriptContext → physics → world.update → send → Sleep-based tick limiter; Ctrl+C clean shutdown; 5s status line. Deliberately skipped headless: terrain/ocean/scatter + terrain collider (camera-centered streaming — the test scene carries its own ground), buoyancy, audio, spatial entries (only RenderComponents register, so radius queries see nothing server-side)
* SNAPSHOTS (server → clients, Unreliable ch0): server streams registered entities' LOCAL pos/rot at "Network/Snapshot Hz", chunked under "Snapshot max bytes", capped by "Max entities per tick"; per-record staleness dropped per entity by tick (chunk reordering harmless — deliberately not UnreliableSequenced). Change-detected against last-sent (epsilon tweaks) + a keyframe rotation (`serverTick % "Keyframe every ticks" == netId % ...`, default 20) so late/drifted clients converge. Rotation smallest-three quantized behind a live "Quantize" toggle carried in the header. Clients simulate locally and correct in `NetworkComponent::update` per the "Network/Correction" tweaks (targets published by `receive()` pre-pass, component reads only)
* OWNERSHIP: everything is server-owned by default; `netId` is minted only by the server, never authored (`Component Network` in .pre is pure presence, no children). `registerEntity` on the server creates/extends the root's spawn record (prefab, id base, transform), announced as a reliable Spawn and REPLAYED to late joiners — that replay is how the world arrives at connect. The Spawn message also carries the root body's live velocities (sampled at send, i.e. AFTER same-frame launch impulses; the receiver caps + applies them) so a replicated projectile's local twin flies instead of dropping from rest — corrections near the receiver's own capsule are grace-suppressed, so the initial velocity is the only thing moving it there. Clients adopt server ids in tree-DFS order; any other registration returns netId 0 = local-inert (never synced) — so client-only/server-only entities are just entities without a NetworkComponent, and id conflicts are structurally impossible. Static scenery that never moves should carry NO NetworkComponent. Unregistering the base id sends Despawn. Never store NetPeerIds on entities (they recycle); clients get a stable `clientId` at Hello/Welcome. `setOwner(root, clientId)` must run in the spawn's own frame (the record rides `m_dynamicRootIds`, purged at send()); later transfers use the per-entity OwnerChange message. `NetworkComponent::authority()` derives Local/ServerOwned/LocalOwner/RemoteOwner (worker-safe)
* CLAIMS (owner → server, Unreliable ch3): the owner simulates freely and streams input + claimed pose/velocities, PHASE-LOCKED to the physics step (`send()` fires them only on frames where `getStepCount()` changed; "Max update Hz" is only a thinning limit, counted in WHOLE STEPS (`stepCount - lastClaimStep >= round(stepHz / maxUpdateHz)`) and never against the wall clock — a step boundary falls partway through a frame while the net clock advances in whole frames, so a time comparison at the step rate measures just-under-interval and DROPS that step’s claim, which the passthrough extrapolates over and double-jumps: remote-client pulsing that vanished only when the limit was raised until it stopped binding; paused physics = no claims, correct). Each packet carries the last "Claim redundancy" claims (default 4, ring cap 8; redundancy beats reliable under loss); the packet record count comes from `validCount`, never seq, so a re-acquired entity can't replay a previous owner's poses. Quantized like snapshots with live ranges in the header. Claims follow the sleep policy (`sleepDirty`): every awake tick, one rest-pose claim on the sleep edge, then silence
* VALIDATION ("Network/Validation" tweaks): movement token bucket in metres refilled at "Max speed" per second of SERVER WALL CLOCK (never per-claim — packet rate is attacker-controlled), linear + angular velocity caps on EVERY accept path, trajectory raycast ("Path raycast" toggle), hard teleport cap; the first claim seeds the anchor but is still checked. "Max speed" (default 60) must exceed the fastest legitimate motion including free fall. Every float off the wire is finite-checked and quaternions re-normalized (NaN passes comparisons and poisons the solver). Accepted → the twin FOLLOWS the claim through the solver ("Twin follow gain", bounded `NudgeVelocity` = claimed velocity + corrective velocity toward the claimed pose; hard teleport only past "Twin resync (m)"). It is deliberately NOT teleport-pinned per claim: that collapsed the render interpolation (prev/curr both became the claim pose) and, since claims are sampled on the OWNER’s step clock while the server steps on its own, yanked the twin backwards whenever two server steps fell between two claims — pulsing visible on the SERVER’s own view. Other clients are unaffected either way because the passthrough sends them the owner’s stream, not the twin. `lastAcceptedClaimPos` anchors the next budget and re-emit to other clients via CLAIM PASSTHROUGH (the snapshot re-emits the newest accepted claim state instead of sampling the wobbling twin; extrapolates by claim velocity for a bounded gap; "Owner predict (ticks)" adds a constant forward shift at emit). Rejected → `violations`++ + `NetRecFlag_Forced` on snapshots for "Forced ticks"; the owner ignores corrections UNLESS Forced, an accepted claim clears Forced immediately, and `updatePlayerControl` yields input while Forced (steering against the correction is a tug-of-war)
* INTERACTION GRACE ("Interaction radius"/"linger"): server-owned bodies near OUR claim-driven bodies suspend corrections (they'd fight the push the player is applying; the twin gets the same push an RTT later). Proxy positions published in `receive()`, read from workers
* TRANSFER/STEAL/ARBITRATION ("Network/Ownership" tweaks, "Transfer enabled" kill switch): awake server-owned dynamics within "Transfer radius" of a client's PRIMARY hand over via per-entity reliable OwnerChange (server resets that entity's claim/validation state); revert after "Release delay" outside "Release radius". Primaries only as transfer sources (`transferredOwnership` distinguishes; client-side it means "owned but not the player" — `updatePlayerControl` skips it). The SERVER's own player body registers as a primary too (`setServerPrimary`, source clientId 0; the Game layer marks it at spawn): never handed to a client, it RE-CLAIMS transferred objects by proximity (only ever transferred ones, never a client's primary), and its snapshot records carry `NetRecFlag_ServerPlayer` — observers route it into the remote-player INTERP ring and skip the interaction grace (it is an actively-steered body: grace-freeing it for local shoves only fabricates divergence, the shove reaches it through the server sim like pushing another client's player). Contact-driven STEALING (`stealOwnershipOnContact`, wired as the primary's `onContact` in main.cpp; needs `ContactEvents true`): last collider owns, never steals another client's primary. Primary-vs-primary contact ARBITRATES both for a window (twins go solver-owned, claims apply as bounded `NudgeVelocity`, records carry `NetRecFlag_Arbitrated`, input keeps steering); an object touched by two distinct clients within "Contest window" is CONTESTED → server-owned until the window decays. Late joiners get OwnerChange replays after the Spawn replay; a disconnect reverts transfers before tearing down primaries. `setOnClientJoined/Left` (main.cpp) spawns/tears down `Entities/Debug/netPlayerCapsule.pre` per client (upright `LockRotation` capsule, same movement/camera as the local player capsule)
* PHYSICS SYNC: a dynamic-body entity syncs the BODY (world pose + velocities, `NetRecFlag_Physics`; vels 3xu16 over live "Max vel"/"Max ang vel" ranges carried in the header). Server sends every awake tick, one sleep-edge record, then keyframes. DEFAULT client correction is the PHYSICAL PUSH: error × `Push pos/rot gain` becomes corrective velocity on top of the server's (capped; delivered as bounded impulses) so contacts and gameplay impulses COMPOSE — no teleport on this path, bodies still sleep inside the deadzones. Past the snap thresholds it enters CATCH-UP (boosted gains, still through the sim); hard `teleportBody` resync only at "Pos teleport threshold". EVERY teleport must also stomp `PhysicsComponent::prevPos/currPos/prevRot/currRot` AND claim the step (`physics->lastStep = getStepCount()`). NetworkComponent::update runs BEFORE PhysicsComponent::update on the same entity, so on any frame the sim stepped it would otherwise overwrite curr with `body.getPosition()` — which still holds LAST frame’s teleport, since teleports apply at the next physics.update — and render mix(thisFramePose, lastFramePose), i.e. BACKWARD, on stepping frames and correctly on the others. That alternation was the remote-entity pulsing. REMOTE-OWNED entities (other players) use BUFFERED SNAPSHOT INTERPOLATION instead ("Remote interp (ticks)", min/default 2 — must exceed 1 or playback degenerates to snapshot arrival; buy smoothness with snapshot RATE, not depth): `NetSnapshotRing` per entity (manager-owned, main-write/worker-read), playback at `newestTick − interpTicks` in server tick units, teleport-following with matched velocities + prev/curr stomps. Asleep records hard-sync once then queue `SetAwake 0`. Kinematic/static bodies use the entity-transform path
* EVENTS: `fireNetworkEvent(name[, data])` = local fire + Reliable ch1 (server relays to other clients). Payload ≤ `MaxEventDataBytes` 1024, static_asserted against the transport’s `netMaxSinglePacketMessage()` so a max-size event still rides in ONE packet; readable during dispatch via `currentEventData()`/`currentEventSender()` (script: `networkEventSender()`). Sender clientId is SERVER→CLIENT only (the server derives it from the receiving peer and re-serializes the relay — never trust a client-writable identity); a client-supplied sender netId resolves through `findOwnedEntity` (null unless that client owns it). `setEventFilter(clientId, name, data, senderEntity)` gates client-originated events (return false = dropped, not relayed); no filter = allow all — install one before any event grants state. Script event sends queue under a mutex, drained by `send()` (NetHost is main-thread-only)
* Session: 4-way transport handshake gates on `GameProtocolId` (bump on ANY wire change; NetFuzz's game mode mirrors it), then Hello→Welcome on Reliable ch2 marks the peer ready. Per-peer state MUST clear on Disconnected. Events (ch1) have NO ordering vs the ch2 Welcome: a client PARKS events that arrive before its Welcome is processed (`m_preWelcomeEvents`, bounded, replayed in order right after) — the server's join-time burst (game world replay, tweak sync) is fired in the same frame as the Welcome and was silently lost before this (already acked = never resent). Anything join-critical sent as an event relies on this parking. Encryption is on by default at the NetworkManager layer (`s_encrypt = true` → `NetHostConfig::encrypt`, whose own default is false); it can't change on a live host, so `setEncryption()` before start, driven by `--no-encrypt`; both ends must match. Without it a peer is identified only by source address (trivially forged on a LAN)
* Testing: server key U throws `Entities/Debug/netPhysCube.pre`; client key C toggles player control (see Testbed keys); key K fires "NetPing". Demo `Entities/Debug/networkTest.pre` spawns at a FIXED transform (never camera-relative — both worlds must match). Link-sim tweaks (loss/latency/jitter) bind into `m_host.config()`; window title shows role + RTT/loss/KB/s
* PLAYER CONTROL (key C on a client, `InputControls::updatePlayerControl`): WASD/Space velocity-steers every locally-owned dynamic body camera-relative (`Network/Player` tweaks: Move speed/Accel/Jump speed — keep Move speed under Validation's "Max speed"); Space jump gated on a downward raycast; fly-camera movement released while active. The owning client simulates its body directly (main thread, before physics.update — direct setters sanctioned there), writes intent into `comp->input`, and the claim stream carries the resulting state

## Script
* Visual scripts compile to real DLLs: `ScriptHost` (`Globals::scriptHost`, Core-only) compiles a .scr with the installed MSVC toolchain (vswhere → vcvars64.bat, `cl /LD /std:c++20 /MD /Od /Zi /DSCRIPT_BUILD`, `/FI ScriptAPI.h`) into `Assets/Local/Scripts/`, caches by path, hot-reloads (`getOrLoad(path, forceRecompile)`) — F6 or a Script Editor save; live entities hot-swap. On failure the previous build is kept. A .scr is BODY-ONLY C++ (no `#include`/`#define`)
* COOKED alternative (`SCRIPTS_STATIC`, off by default): `Code/AppScripts` generates one aggregate TU that `#include`s every `.dsl` (only — .scr is not cooked) into its own namespace and compiles it INTO the engine (`/WHOLEARCHIVE:AppScripts` keeps the registrations). `REGISTER_*()` markers at the end of each .scr feed a static-init registry `getOrLoad` resolves from instead of cl/LoadLibrary — whole-program-optimized. All-or-nothing: one broken .scr fails the aggregate
* A .scr IS the generated C++ (entry points `OnSpawn`/`Update`/`OnDestroy`/`OnEvent`/`OnPhysicsEvent` + `ScriptEventCount`/`ScriptEventName`/`ScriptDataSize`) with the node graph appended as `//@graph`/`//@node` comments — the NodeEditor regenerates code from the graph; the file is also hand-editable
* .scr / the NodeEditor path is REMOVED from the running editor: no node panel, no `UI::m_scene`, no .scr open/create routes (the `NodeEditor/` sources remain in the tree for reference and still compile as unused partitions). A .scr shows as a plain text file in Content, isn't offered by script pickers, and isn't cooked; `ScriptHost` itself would still compile one if a .pre carried the path, but nothing authors that anymore. When Claude is asked for a script, author a .dsl via DslCompiler — never a .scr
* `ScriptAPI.h` is the host↔DLL ABI: C linkage, PODs/glm/raw pointers only. The `ScriptContext` function table is APPEND-ONLY (cached DLLs must keep working). Scripts see a layout-compatible `Entity` MIRROR (pos/scale/rot/parent only; offsets static_asserted in ScriptContext.cpp). Components a script can't name go through opaque handles: `ctx->entityGetForceComponent(entity)` returns `void*`, `ctx->forceGet*/forceSet*(handle, ...)` cast back host-side (the NodeEditor hoists the handle so lookup runs once)
* `ScriptModule`: DLL entry pointers + event names + data layout (`dataLayoutId`, `dataFields`, `requiredComponents`); don't cache function pointers across reloads
* DSL surface: `math.*` namespace (all scalar math, ANGLES IN DEGREES; `ocInverseLerp/ocRemap/ocMoveTowards/...` in ScriptAPI.h avoid double-evaluation; randomness is one ENGINE-side generator so hot-reload doesn't reset it). `self.animator.setFloat/setBool/setTrigger/...` — animation is the animator's surface, nothing names a clip. A getter-call member MUST register `writable=false` (a writable member's emit is used verbatim as an assignment target)
* ScriptData: `//@@data [private|public] <type> <name>` fields (default Hidden); exposed fields surface through `ScriptDataFields()` (`OcScriptField[]` — the only engine-side layout description). Properties panel edits them LIVE (never serialized); Entity Editor authors INITIAL values (text under `Component Script`/`Data` in .pre, re-applied by `applyInitialValues` after every `syncScriptData` reallocation)
* DSL containers: `T[]` arrays live ENGINE-side (`OcArray` generation-tagged handles; storage survives hot-reload). No unchecked reads: `foreach [ref] T x in c` iterates, `ifexist [ref] T x in c [at k]` is the single checked lookup. POD arrays resolve once to a span (`ctx->arraySpan`) — one ABI call per loop, elements are plain loads, `ref` binds a real C++ reference into the span; Entity/String arrays keep checked per-element reads. THE SPAN IS ONLY VALID WHILE THE ARRAY DOESN'T GROW — `checkContainerMutations` (ScriptLoader) enforces it as a whole-document fixed-point over the call graph (a push through a parameter poisons the function for every caller, conservative on purpose) and runs from BOTH `save()` and `load()` (the editor's Compile & Run path never parses); push/clear on a container an enclosing loop reads is refused. `ref` is refused on handle types and non-writable containers; non-`ref` elements can't be assigned. The editor-side grey-out (`ScriptLang::isContainerIterated`) is still syntactic — transitive mutation through a user function is authorable and gets refused at load
* `//@@require` is enforced LIVE by `requirementsMet` (mask test) at every entry point — not latched at spawn (hot-reload mutates the module in place). `OnSpawn` runs once, the first frame the entity qualifies (`onSpawnRan`); `OnDestroy` pairs with that flag, never the live check. `syncScriptData` re-allocates + refills require slots + clears `onSpawnRan` when the module layout changed; `syncScriptDataLive` (every post-spawn entry point) also re-registers the event listener (its stored pointer would dangle)
* FAULT CONTAINMENT (`OC_SCRIPT_FAULT_CONTAINMENT` at the top of ScriptComponent.cpp, default 1; 0 = raw calls, faults crash normally for clean dumps): every entry-point call goes through SEH-guarded invokers (`invokeScript*` in ScriptComponent.cpp — plain `__try`/`__except`, no C++ exceptions, zero cost until a fault): a hardware fault in script code (int divide by zero, stale pointer) marks the module `faulted` and logs instead of crashing. `requirementsMet` folds the flag in, so the script stops running everywhere (OnDestroy included — its data may be half-written); ScriptEventManager's direct OnEvent path checks it itself. A successful (re)load clears it (`loadDll`), so F6 / Compile & Run is the recovery. Deliberately unguarded: breakpoints, stack overflow, C++ exceptions (filter whitelist in `scriptFaultFilter`). Float div-by-zero never faults (FP exceptions stay masked — inf/NaN propagate)
* There is NO null in the DSL: absent values are OPTIONAL types (`Entity?`, readable only via `ifexist`); dead entities in arrays report a lookup miss
* The DSL subsystem (data model, autocomplete, bindings registry, save/load, C++ transpiler) lives under `Private/DSL/` as partitions re-exported from `Public/ScriptHost.ixx` — editor-agnostic; UI's Script Editor panel (`import Script;`) is its consumer. Types an outside importer needs must be `export`ed (partitions leak unexported types across same-module files — easy to miss)
* `DslCompiler <input> [output.dsl] [--compile]` (`Code/DslCompiler`, console exe) — THE way Claude authors .dsl files: write only the DSL text, the tool validates (same loader as the editor, full binding vocabulary), transpiles, and writes the exact file the editor's Save produces. USAGE + FULL LANGUAGE REFERENCE: `Code/DslCompiler/CONTEXT.md`. Raw input = single-`@` directives (`@require`/`@data`/`@event`) + TAB-indented code; the tool prefixes every line with `//@` (which is what turns `@require` into the stored `//@@require`) and wraps in the markers. An existing .dsl as input = normalize + regenerate C++ in place. `--compile` cl-compiles + loads the output through the real ScriptHost pipeline (the F6 path) — the final check that the script loads in the engine as-is. Errors print `<input>(<line>): <what>`; relative paths resolve against Assets/; exits via `_Exit` (nothing initialized, global dtors assume engine teardown order)

## Physics
* box3d (Erin Catto's 3D engine, C API) wrapped in `module Physics`; box3d linked PRIVATE, never leaks (ids stored as integer bits)
* MULTITHREADED SOLVER (`Physics:TaskScheduler`, `TaskScheduler.ixx/.cpp`): box3d's per-step fork/join is driven by the ENGINE job system, not the private thread pool box3d spawns for itself when `workerCount > 1` without task callbacks (that pool would fight the engine's workers for the same cores). `PhysicsTaskScheduler` is an OBJECT owned by PhysicsWorld and handed over as `b3WorldDef::userTaskContext` — it comes back through the `userContext` parameter of both callbacks, so all scheduler state is per-world and none of it is global (`enqueueTask`/`finishTask` are its two statics). box3d has ALREADY sliced each phase into `workerCount` pieces and baked the slice index into every `taskContext`, so a task is an opaque closure — run it exactly once, on any thread, no worker-index mapping needed (this is why the callback takes no worker index, unlike box2d). Each enqueue is one `EJobPriority::High` job (the step blocks on it, so it is on the frame's critical path); `finish` is `jobSystem.wait(counter)`. Task slots are a FIXED 256-entry (`B3_MAX_TASKS`) ring claimed with one `fetch_add` — no free list, no lock: box3d never enqueues more than `B3_MAX_TASKS` per step and finishes all of them inside that step, so any wrap reuses a slot at least a whole step old, and the slot addresses handed back to box3d stay stable. `finishTask` MUST BLOCK (`b3World_Step` holds its stack across every fork/join) and box3d's rule is "DO NOT call `b3World_Step` and park that fiber" — so the step must run somewhere whose blocking wait HELPS rather than suspends. `PhysicsWorld::update` is main-thread-only and `JobSystem::wait()` helps on the main thread (draining the High queue these tasks just landed in first), which is exactly that; driving the step from a job fiber would satisfy the JobSystem but violate box3d's rule. The partition deliberately does NOT include box3d — the callbacks are spelled in plain C types (`void(*)(void*)`), identical to `b3TaskCallback*` once the typedef expands, so `b3WorldDef` takes them without a cast and box3d still never reaches a Physics interface; the two facts the interface therefore cannot check (`c_maxTasks == B3_MAX_TASKS`, power-of-two) are `static_assert`ed inside `enqueue`. `enqueue` runs the task INLINE and returns nullptr (box3d's "already done, skip finishTask" signal) when there is no job system — tooling builds, and the teardown window where `~JobSystem` (init_seg XCU5) has already run but the plain-`.CRT$XCU` physics global has not, so `b3DestroyWorld` still reaches the callback. Tweak: "Physics/World" → `Worker count`, live via `b3World_SetWorkerCount`, 1 = single-threaded A/B. It defaults to `jobSystem.getNumWorkers() + 1` — the `+1` is the thread DRIVING the step, which box3d counts as a worker and slices for. box3d's own guidance is to size this by PHYSICAL core count — which the job system itself now defaults to, so the default matches; the tweak remains the knob for trimming further (efficiency cores)
* PHYSICS JOB MARKERS (always on, `EProfileCategory::Physics`): each task runs under a `ProfileScope` named by the LITERAL `"Physics Job"` — opened by the JobSystem itself from the submit's `JobProfile` (the same string is the job name, so stats and flame graph agree), so a step's fan-out reads as named spans on the worker tracks. box3d's own `taskName` is DELIBERATELY IGNORED: `ProfileRecord` stores names BY POINTER and the panel reads them back frames later, but box3d builds `taskName` into a TRANSIENT buffer, so the pointer is dangling by draw time and every physics scope rendered as a row of `?????`. Copying the text into the task slot does not fix it either — the slot is reused a step later and the copy is overwritten, so names on already-recorded blocks visibly keep changing. A literal is the only name that still means what it said, and box3d's per-phase detail is not worth a string store on this path. Unlike the per-entity component updates these are NOT rate-limited or gated: the record volume is bounded by the STEP rate, not the frame rate (`B3_MAX_TASKS` per step × the 120 Hz `Step Hz` ceiling ≈ 15.6K records/sec spread across every worker — tens of seconds of ring history per thread against a 512-frame panel window). The BLOCKING side gets one `"Physics fork/join"` marker per `finishTask`, on the thread driving the step: the row of them under "Physics step" IS the step's parallel structure, each one's width the cost of that phase. Because `wait()` helps rather than sleeps, unrelated jobs the main thread picks up to burn the stall nest inside that scope with their own markers — the same honest-but-broad attribution "UI prepare wait" has, and it also lands their allocations under the physics path in the Memory panel
* `Globals::physics` (`PhysicsWorld`): `initialize()` once; `update(deltaSec, contactCb)` per frame (fixed-step accumulator; gravity/paused/interpolate/timescale/substeps/Hz under Physics/World tweaks). `createBody(PhysicsBodyDesc, span<PhysicsShape>)` (Box/Sphere/Capsule/Hull/Mesh; desc scale baked into shape dims), `castRayClosest()` (optional layer mask). Debug draw: App registers a line sink + view-position source once via `setDebugDrawCallback`; `update` emits wireframes while "Physics/Debug/Draw colliders" is on (also Draw joints/contacts/bounds + Range; not gated on paused — inspecting a stopped sim is the point)
* THREADING: `teleportBody(body, pos, rot)` is the ONLY way to move a body (mutex-queued, applied before the first step and before the paused check, handle revalidated via `b3Body_IsValid`). `PhysicsBody` deliberately has NO setTransform. The queue generalizes to a BODY-COMMAND queue (`queueBodyCommand(body, EBodyCommand, a, b, c)` + `queueSetGravity`): velocities, impulses, forces, torques, gravity scale, damping, setAwake, `NudgeVelocity` (bounded network correction), world gravity. EVERY script physics-write thunk queues (scripts tick on workers; even a velocity setter wakes the body into shared solver sets). All other box3d writes are MAIN THREAD ONLY; `PhysicsBody`'s direct setters remain as main-thread API. One frame of latency; reads return pre-write values. The DSL's `self.physics.teleport(position, eulerDeg)` rides the same queue and works on any body type
* Buoyancy: `setWaterSurface` + per-body probes ("Physics/Buoyancy" tweaks) — the App wires the ocean's `sampleWaterHeight` in
* `PhysicsBody`/`PhysicsJoint`/`PhysicsMesh`: RAII movable handles. Joints (`createDistance/Revolute/Spherical/WeldJoint`) take world-space anchors; `staticBody()` anchors to the world. `PhysicsMesh` (triangle BVH) must outlive Mesh shapes; Hull shapes clone. Mesh colliders only collide on static bodies
* `PhysicsComponent`: `Component Physics` in .pre (`Body` Static/Kinematic/Dynamic, `Shape` Box/Sphere/Capsule/Hull/Mesh, `HalfExtents`/`Radius`/`HalfHeight`/`Density`/`Friction`/`Restitution`/`MaxHullVertices`/`Sensor`/`ContactEvents`/`LockRotation` (solver-level lock on all angular axes — upright character capsules, see Entities/Debug/playerCapsule.pre; a locked dynamic body writes back only its POSITION, leaving entity.rot to scripts — player facing yaw); see Entities/Debug/physicsCube.pre). Hull/Mesh geometry comes from the sibling render mesh via `CollisionCache` (a World member — THE asset→physics bridge: snapshots a container's collision geometry from the same ISceneData one import serves, derives hulls/BVHs/occluder sets per container|node, caches each). `Col_*` meshes/nodes are collision proxies (replace the same-named render mesh for physics; never rendered)
* THE BODY OWNS THE POSE: created at spawn from the entity's world transform; physics never reads the entity's position again. Dynamic bodies write the simulated pose back each update (interpolated between fixed steps); non-dynamic bodies stay put. Gizmo-dragging or writing `self.pos` moves only the mesh — `teleportBody` is the only way to move a body. The static-mesh occluder is baked once at spawn (re-baked at the body's pose on resume-from-disabled). Sponza has a static mesh collider
* Contact/sensor events: shapes opt in via `ContactEvents`/`Sensor`; delivered per step through `update`'s contact callback (→ `World::handleContactEvent` → `PhysicsComponent::onContact` + the script's `OnPhysicsEvent` with the other entity, begin/sensor bools, contactId; sensor events have contactId 0). `getContactPoint(contactId, ...)` resolves world hit pos/normal, valid until the next update
* Layers: `PhysicsLayers::bit(name)` allocates one of 64 bits ("Default" = 0); shapes carry category/mask/group. In .pre: `Layer Debris` + `CollidesWith Default, Player` (or `All`/`None`) + optional `Group <int>`. Demo: physicsSphere.pre

## Particle
* GPU particles + projected decals. Renderer side (ParticlePipeline/DecalPipeline, GPU structs + MAX_* in Layout.ixx) lives in RendererVK; this library is the gameplay layer: `Globals::particleSystem` — `initialize()` before world spawns, `update(renderer, dt)` AFTER `world.update` (rates/bursts → GPU spawn requests; re-uploads every emitter config so live .pfx edits retroactively apply; ages + submits decals)
* Particles simulate ON THE GPU (`particle_begin/emit/sim.cs.glsl`): one persistent pool (MAX_PARTICLES 256K), dead-index stack, two alive lists ping-ponged by frame parity; all dispatches indirect and the OUT alive count IS the draw's instanceCount — spawning never re-records. Sim: gravity/drag/noise turbulence + optional screen-space collision vs last frame's G-buffer; billboards draw in scene color (velocity stretch, flipbooks, per-particle GI+sun lighting, soft depth fade, premultiplied blend with per-emitter additivity 0=smoke..1=fire) in ONE pipeline. Tweaks under Particles/*
* `ParticleEffectDesc` = named set of emitter descs, loaded from `.pfx` text (grammar documented in Effect.ixx; demo Assets/Effects/fire.pfx), cached by path; `createEffect(pathOrDesc, pos, rot)` → RAII `ParticleEffect` (setTransform/setVelocity/setEmitting/burst; destroy retires live particles via KILL flag). Renderer slot API (`createParticleEmitter`/..., MAX_PARTICLE_EMITTERS 256) is main-thread. Textures via `Renderer::loadEffectTexture` into the bindless array
* Decals: `spawnDecal(DecalDesc, pos, normal)` — aging pool (lifetime 0 = persistent, `removeDecal` fades out; oldest non-persistent evicted at MAX_DECALS 4096); pushed per frame via lock-free `Renderer::addDecal`. One instanced draw of unit cubes after the opaque pass (front-cull, no depth test): FS reconstructs the surface from G-buffer depth, projects into decal space (+Z axis), angle-fades, blends premultiplied; DECAL_FLAG_LIT approximates sun + GI. Wraps static AND skinned geometry (pure screen-space)
* `ParticleComponent` (`Component Particle`: `Effect <path.pfx>` + optional `Emitting false`; demo Entities/Debug/particleFire.pre) follows the entity transform and feeds finite-difference velocity

## Force
* Forcefield bubbles: emitters (Team 0-7, Output, Reach, Focus, Distribution, Width + direction) project ANALYTIC fields with compact support spanning exactly `pos .. pos + dir*Reach`. Focus 0.5 = exact sphere spanning the line, 0/1 = exact cones; Width is a pure lateral scale; `distGain` bumps density along the line. TOTAL output is invariant across shape params (a quadrature-cached budget fold pre-applied to the uploaded Output — pinching densifies instead of shedding power); reach still scales the total. Gradients finite-differenced. Same-team fields SUM (metaballs); the drawn surface is the equal-field equilibrium `phi_best - max(iso, phi_second) = 0` — squish/pierce fall out of the math. Field math lives ONCE in `force_field.inc.glsl` (CPU lobe mirror in System.cpp drives the debug rings)
* `Globals::forceSystem`: `initialize()` from main (registers "Force" tweaks → `ForceFieldParams` pushed every update); `update(renderer, dt)` main-thread after world.update = the only writer of renderer force state. `createEmitter(...)` → RAII `ForceEmitter` (setters safe from the parallel pass — own slot only; `getAppliedForce()`/`getPressure()` = GPU readback ~2 frames latent); `createQuery(pos)` → `ForceQuery` (territory: `Result{owningTeam, inside, ownField, opposingField, valid}`, same latency)
* Renderer side (`RendererVK:ForceFieldPipeline`): emitter/query slots main-thread with free-list + retirement (readbacks slot-indexed so slots never re-pair with stale results). Rendering = one instanced draw of unit cubes (decal pattern, after debug lines / before particles) whose FS ray-marches the field through the emitter's reach box, bisection-refines, manual reversed-Z depth test; OWNERSHIP DISCARD dedups merged bubbles. Compute (`recordForceCompute`, after light grid): uniform 32 m emitter hash grid per frame (`force_grid.inc.glsl` + `force_grid.cs.glsl`; emitters over "Big reach threshold" bypass into a ≤64 global list); `force_emitter.cs` integrates opposing pressure (13-sample integral — a center-only tap reads zero when big bubbles press rims), `force_query.cs` evaluates points; host-visible readbacks. "Force/Use grid" off = brute-force A/B toggle
* `ForceComponent` (`Component Force` in .pre: `Team`/`Output`/`Reach`/`Focus`/`Distribution`/`Width`/`Centered` + local `Direction`/`Offset`; demo Entities/SphereField.pre) keeps its emitter on the entity's world transform (not frozen-gated — placement, not simulation). `Centered` (default true) pulls the emitter back Reach/2 so the bubble centres on the entity. Reach is world units, unscaled by the entity. No authored on/off — scripts use `setOutput(0)`. Script access via the `entityGetForceComponent`/`forceGet*`/`forceSet*` handle ABI; NodeEditor Get/Set Force nodes
* `ForceEmitter::getEquilibriumRadius()`: pressure-aware CURRENT bubble radius estimate — the closed-form iso profile at the widest station with the threshold raised to max(iso, pressure readback). An AVERAGE (the true surface sits closer on the enemy-facing side), ~2 frames latent. The Game player's shield UI/damage logic reads it
* `ForceEmitter::setShellAlpha(alpha)` (`outputParams.y`, default 1): shell opacity; 0 SKIPS the ray-marched shell draw entirely while the field stays live (deforms bubbles, force/pressure/query readbacks). Invisible fields also never TINT other shells: shading weighs team color + contact glow by alpha-scaled fields (`forceAccumulateVisible`), and an equilibrium wall against an invisible side reclassifies as the visible team's SKIN (shaded + owned by that team — the invisible proxy never rasterizes, so pane-classified walls would silently drop). `ForceFieldPipeline::upload` partitions the compaction drawable-first and sets the draw instanceCount to the drawable count only (compute dispatches keep the full count). This is how a map-scale invisible emitter (the Game world field) avoids a full-screen march — combined with the big-emitter list it needs NO other special support

## Spatial
* High-performance spatial index for culling and position lookup, independent of the entity hierarchy. Implicit 64-ary hierarchy over per-level hashed grids — 11 levels, cell size ×4 per level (finest = compile-time `SPATIAL_FINEST_CELL_SIZE`, default 2 m → ±2097 km), BMI2 Morton keys (`Spatial:Morton`), each `CellRecord` holds a 64-bit child-occupancy mask so queries descend with bit scans. API takes dvec3 (quantized to an int64 lattice); per-entry data is SoA with cell-relative float positions; query math is reference-relative float — exact at planet scale
* `Globals::spatialIndex`: `initialize()` before world spawns; `registerEntry(pos, radius, userData, layerMask)` → `SpatialHandle`/`SpatialEntry` RAII; `updateEntry` per frame (same-cell rewrite ~4 stores; cell changes staged into PerWorker lists — callable from any job, one visitor per entry; register/unregister/commit/setLayerMask are main-thread); `commitFrame()` once per frame drains + sweeps. Queries read-only between commits: `querySphere/queryAABB/queryFrustum/queryRay/queryNearest`. Two visibility stamp sets (`markVisibleSet(Main, frustum)` + `markVisibleSphere(Near, ball)`; `getPassMask`/`isVisible`); the App drives all of it through ONE call, `SpatialIndex::update(camera, frustum, viewProjRelCamera)` (main loop after beginFrame: commit + cull-dist + mode/freeze gate + margin-inflated Main stamp with occlusion + Near ball with requery hysteresis)
* Entity integration: every RenderComponent auto-registers on spawn (bounds from `RenderNode::getWorldBounds`, skinned × "Skinned radius scale", layer `SpatialLayer_Render`); update pushes per-pass masks — Main-visible entities push `PASS_ALL`, Near-only push `PASS_SHADOW|PASS_GI` so shadows/GI keep off-screen entities. `Spatial/Culling/Mode`: Off / Stats only / Cull (default) / Main only (debug — visibly kills off-screen shadows/GI); `Freeze` stops re-stamping to inspect the culled set
* CPU software occlusion (`Spatial:Occlusion`, `Globals::occlusionBuffer`, Spatial/Occlusion tweaks, default off): the largest triangles of static mesh colliders (from CollisionCache occluder sets, registered by PhysicsComponent, RAII `SpatialOccluder`) rasterize camera-relative into a 256x144 CPU depth buffer + 8x8 max-depth blocks; plugged into the Main stamp as `IOcclusionTester` — occluded cells drop from the MAIN pass only. Conservative toward visible
* Static tier: entries unchanged for N frames (Spatial/Static tweaks) promote into per-level Morton-sorted SoA ranges (`StaticStore`), budgeted one level per commit; any change demotes. Sphere/AABB/frustum testers all test static ranges 8-wide (AVX2 `test8`)
* Stress harness (`Globals::spatialStress`, Spatial/Stress tweaks): synthetic entries on `SpatialLayer_Stress`, churn, timed queries, brute-force verify; stats under Spatial/Stats
* Scripts: `ctx->spatialQueryRadius` / `ctx->spatialGetNearestEntity`; NodeEditor "Get Nearest Entity" + "For Each Entity In Radius"
* Planned (not yet built): Threading-parallel traversal (queries are read-only between commits, so parallelFor over cells is safe once needed)

## Nav
* Sparse per-TEAM flow fields for crowd pathfinding (`Code/Nav`, module `Nav`, partitions `:Grid`/`:ChunkMap`/`:Field`/`:Density`/`:System`; `Globals::navSystem` in init_seg `OC_SEG_NAV`, first to destruct — its dtor waits on in-flight build jobs). Entity-agnostic: the GAME feeds `NavObstacle` AABBs + per-team `NavSource`s (pos, stopRadius = footprint half, id, kind 0 structure / 1 player), UNITS read the results inside the parallel entity pass
* Grid: 2D over XZ, 2 m cells (= the structure grid = Spatial's finest cell), 16×16-cell CHUNKS (32 m) in `Nav::ChunkMap<T>` (open-addressed uint64→unique_ptr<T>, the Spatial CellMap pattern; heap-owned values so growth never moves a chunk). Chunk key = 28-bit packed chunk coords (`Nav::chunkKey`); everything in `Nav:Grid` is pure coordinate math. Storage scales with ACTIVE AREA, not world size — several far-apart battle sites coexist
* `TeamField` (immutable once published, one per team): per chunk `cost[256]` (u8: 0 free, 255 blocked, else an extra step multiplier — a clearance ring of "Clearance cost" hugs walls off), `dist[256]` (u16, 1/8 m, geodesic distance to the NEAREST source of that team) + `src[256]` (winning source index). Built by radius-bounded multi-source Dijkstra (8-connected, no corner cutting, seeds = every cell of each source's footprint, stops past "Field radius") — the front CREATES chunks as it goes, so a field is exactly as big as its reach. The build jobs are internally MULTITHREADED: `rasterizeObstacles` pre-creates every reachable chunk serially, then parallelFors the Blocked stamping (per obstacle) and the clearance ring (per chunk) — racing writers all write the same byte value — and the flood itself is a CHUNK-WAVE Dijkstra: each wave parallelFors `floodSolveChunk` over the dirty chunks (a 256-cell mini-Dijkstra to the LOCAL fixpoint — 3×3 chunk pointers resolved once, all cell access by index, writes stay inside the chunk, a local lazy heap of packed (dist<<16|idx) u32s), then a serial step turns each solve's `borderImproved` mask (8 dirs) into the next wave, creating chunks the front wants to enter; distances only decrease and the relaxation fixpoint is unique, so waves converge to EXACTLY the serial result (cross-chunk reads race benignly — a missed improvement just re-queues the chunk). "Nav build wave" is the per-wave worker span. NOTE for anything running inside these jobs: gather buffers must be STACK locals, never thread_local — a parallelFor wait parks the fiber and the worker may pick up another team's job, which would clear a shared thread_local mid-use. `sample(xz, seed)` = 3×3 lowest-neighbour scan crossing chunk borders (seed-jittered ties so plateaus don't stall a crowd) → `{valid, dist, srcIndex, descentDir}`; `sourceAt(i)` names the target
* `NavSystem::update` (main, from `GameMatch::feedNav` after `service`, i.e. before the next entity pass): publishes finished builds (`shared_ptr<const TeamField>` swap — workers only ever read published pointers, which change only here), kicks one Low-priority `navFieldBuild` job per team when its sources changed (id/kind/moved > half a cell), the obstacle raster changed (change-detected by hash in `setObstacles`; the obstacle snapshot is shared by every job so it only swaps when NO build is in flight), the "Rebuild interval" elapsed, or nothing is published; a team with no sources publishes null. the per-team `FlowField`/`PressureField` (`Nav:Density` partition — the name is historical) use the same chunk machinery: a splat into a missing chunk queues the key via `PerWorker` and main creates it next frame; the field STEPS run MULTITHREADED PER CHUNK across ALL teams (`beginStep` per field on main = drain/flip/evict/gather + `prevActive` snapshot, stashing the step params on the field; then ONE `parallelFor` over every team's flow chunks, a barrier, ONE over every pressure chunk, then `endStep` flips pressure — two barriers total; a chunk task writes only its own write buffer/peak/touched and reads neighbours' READ buffers, the pressure→flow push splats atomically into flow buffers the flow phase has already settled, and pressure growth requests ride the PerWorker touch queue, materializing next update); fields evict chunks idle for "Chunk keep frames"
* GOAL fields (`setGoal(slot, dest, radius)`/`clearGoal`/`goalField`, `MaxGoals` 4): the SAME TeamField with ONE source (kind 2 = a destination), non-periodic — rebuilt only when the destination moves > half a cell or the raster changes, radius per order. Slot 0 = the local player's RMB move order (`GamePlayer::tickMovement`: sets the goal each frame while an order stands, steers by the field's descent when it covers the capsule, straight line until then; cleared on arrival/no order). Works on CLIENTS too: `feedNav` runs there with obstacles only (walls + mirrored structures, no sources), so the goal field is local. "Debug team" 8 draws goal slot 0
* Unit integration (`GameUnitComponent::update`) — CONTEXT STEERING over the fields, no search and no per-unit path state beyond the last heading: the ENEMY team fields (every other team sampled at the unit's cell, geodesically nearest enemy wins → `targetPos` + `goalDir` = its `descentDir`; skips the spatial-query target search; a unit is a source of its own team's field so unit-vs-unit works the same way; no covering field → old local search, `goalDir` = straight at the target; route waypoints / DSL-locked targets → `goalDir` straight at them) give the GOAL direction, then each tick 16 candidate headings ANCHORED ON THE GOAL (k = 0 IS `goalDir` exactly; the other 15 carry a per-unit offset under half a slot, so a crowd sharing one goal heading does not deviate onto the same sixteen world directions and walk in columns) PLUS the exact lane direction are scored — a one-cell gap subtends about one 22.5° slot from a few metres out, so a free-floating fan often had no candidate pointing through it: `free·(Goal·dot(goal) + Flow·dot(lane)·w(|lane|) + Persist·dot(lastDir)) − Pressure·dot(∇p̂)·w(|∇p|) + Wall·dot(wallAway)`, where `w(x) = x/(x+knee)` is a COMPRESSIVE response ("Pressure knee" 0.2, "Flow knee" 0.15 × moveSpeed — low, so even a half-faded lane still counts as "there is a lane here") — steep at low values so ONE stuck unit registers, flattening toward 1 so a hundred never saturate a cell — and `free` = open fraction of a "Look-ahead" CENTRE-LINE march (a 1-tile gap reads as open) and `wallAway` = a proximity-weighted wall push — BOTH read a `TeamField::CostWindow`, the per-tick stack SNAPSHOT of the raster around the unit (`snapshotCosts`: a few chunk lookups + row memcpys, then every probe — marches, body/corner samples, wall push — is a plain array read where the chunk-hash helpers paid a find() per cell, 100+ per unit per tick; cells beyond the window read as OPEN, so size the radius to the longest probe) — `wallPush` (proximity-weighted push away from blocked cells within body radius + "Wall keep" 0.6 m — in a gap both walls cancel and the unit centres itself, at a corner it swings wide); a candidate whose cell one body radius ahead is blocked is out, and two LATERAL body samples at that point subtract "Corner clip penalty" each — the run is measured on the centre line so a one-cell gap stays usable, and clipping a corner is discouraged rather than forbidden. SEED PATH: `NavSystem::seedPath(team, from, to, speed, width)` plans ONE route with A* over the raster (`TeamField::findPath` — straight-shot test first, octile A* capped at 8192 cells, string-pulled by `lineOfSight`) and WRITES it BOTH into the team's flow as a lane (`FlowField::seedPath`, max-magnitude into both buffers — plus an override when the new vector is more than 60° off the existing one, so a RE-plan down a different route wins even where a milling crowd's splats sum higher than the lane speed — so it is visible immediately; the written VECTORS blend between the two segments meeting at each junction over 2 m, so a 90° corner reads as an arc while the polyline stays put (a rounded path could cut a wall corner) — and a vector that would AIM AT A WALL falls back to the pure segment direction, since at a one-cell gap the blend otherwise points at the wall beside the opening and units follow it there; "Lane width" metres wide (0 = one cell; blocked lateral cells hand their share to the surviving ones — a lane squeezing past a wall FOCUSES on the centre instead of thinning, capped 2x), written 1.6x stronger at CONSTRICTIONS (a cell with a blocked lateral neighbour) since a gap has no surrounding flow to support it and a crowd waits at its mouth — separate from the planning CLEARANCE that keeps the route off walls; blocked cells are skipped) AND as a NEGATIVE-pressure TROUGH along the same route (`PressureField::seedPath`, depth "Nav/Seed trough" deepened by 1 + 4x "Nav/Seed trough squeeze" ONLY in a strictly ONE-CELL gap — both immediate lateral neighbours blocked; wider gaps, diagonal staircases and a lane merely running ALONGSIDE one wall stay plain; min semantics = idempotent) — pressure is where ATTRACTION lives (the steering reads −∇p and the flow is pushed by −∇p), so the lane pulls units and the surrounding flow INTO itself instead of only existing where it was drawn. Only the first "Nav/Seed range" metres (40) of a plan are WRITTEN — the A* still runs to the real destination, since a truncated search would pick the wrong way round an obstacle, but a lane far ahead of the group is stale by the time anyone gets there. Consumers: (a) a fresh RMB move order for the selected units, started at the centroid of their LARGEST CLUSTER (single-linkage flood fill at "Group cluster radius" 12 m — a straggler must not drag the lane's origin off the bulk), (one plan, at the press — the units' own periodic requests below keep it fresh afterwards), (b) every barracks route change (`GameMatch::seedRouteLane`, leg by leg), (both at "Order lane speed/width" — a player order writes a strong wide lane the group commits to), and (c) EVERY unit on its own timer: while it has somewhere to be, a unit queues a `SeedRequest{from, to, team, stuck}` every "Seed request interval" (jittered ×0.75–1.25, random phase at spawn), drained on the main thread by `NpcSystem::service` into `NavSystem::requestSeedPath` at "Order lane speed" — or the weaker "Stuck lane speed" when the unit is stalled past "Unstick after". `requestSeedPath` is the RATE LIMITER that makes a per-unit request affordable: it refuses a plan when one of the same team was already made within "Nav/Seed area" (16 m) of BOTH its start and its destination inside "Nav/Seed cooldown" (4 s), and caps plans at "Nav/Seed max/frame" (2). The test is a DISTANCE (a grid-bucket test let two units either side of a border both through), but the candidates come from a hash of recent plans BUCKETED BY START at exactly that radius, so only the 3×3 bucket neighbourhood is scanned — constant cost per request no matter how many units ask or how many plans are alive, and stamps retire through an EXPIRY QUEUE (one FIFO of (time, bucket); stamps are made in time order and buckets are append-only, so `update` pops exactly what expired and never walks the map). A crowd walking the same way therefore costs ONE plan, and the group's lane keeps up with the group as it advances without anyone tracking the group (there is no group re-seed timer). Nothing per-unit plans — the crowd follows the seeded lane, which then decays like any other. STALLED (position checkpointed every 0.75 s; < 0.6 m moved since the last checkpoint accrues stalled time, else it resets — displacement, not per-tick progress, because a jittering heading never accumulates progress) shifts the weights (Goal ×0.3, Persist ×0.2, Flow ×2) so the fields become the preference when the unit has problems; past "Unstick after" (1.5 s) Persist drops to 0, Pressure ×3, and the GOAL is replaced by an ESCAPE goal (weight 1.5) that BACKS AWAY from whatever pins the unit: away from a nearby wall first (the CostWindow wall push), else away from the nearest unit/structure/player found by a small 4 m spatial query (scenery filtered out), else simply the reverse of the intended direction; plus a ±30° per-unit random rotation — forced movement out of corners that also breaks symmetric two-unit locks. Tweaks "Game/Enemies/Steer": Goal 0.7 / Flow 1.5 (a lane is a proven route — where one exists it outweighs walking straight at the goal) / Persist 0.8 / Pressure 0.5 / Look-ahead 6 / Corner clip penalty 0.4 / Wall push 1.2 / Wall keep 0.8 m / Pressure knee 0.2 / Flow knee 0.35 / Order lane speed 4 / Stuck lane speed 2 / Lane width 2 m / Seed request interval 5 s / Group cluster radius 12 / Presence pressure 0 / Stuck pressure 0 / Unstick after 1.5 s / Order flow blind 0 s (Synced; Goal below 1 is deliberate — at 1.0 the goal term overrode the lane term at walls). The lane is sampled with `FlowField::sample(pos, raster)`, which leaves BLOCKED cells out of the 3x3 average instead of averaging them in as zero — inside a gap two thirds of the neighbourhood is wall, which used to cut the lane's apparent strength to a third. CONTRIBUTIONS BACK: the two pressure INJECTIONS ("Presence pressure" per unit per tick, "Stuck pressure" while stalled) both default to 0 — the pressure field now carries the seeded lane TROUGHS, and a crowd's own positive haze only muddied them; being stuck REQUESTS A PLANNED PATH instead (below), which is a real answer rather than a repulsion field. Both tweaks remain for re-enabling. Moving units splat their MEASURED planar velocity into the FlowField ONE CELL BEHIND them (a unit samples its own cell at double weight, so splatting there fed its heading back to itself and biased everything toward going straight; a trail belongs behind the walker) (`Nav::FlowField` per team: atomic int16 EMA — splat × (1−step), write buffer = read × step where `step = 0.5^(dt/"Nav/Flow half-life")` (10 s — a seeded order lane is still ~50% after 10 s), i.e. FRAME-RATE INDEPENDENT and configured as a half-life in seconds, not a per-frame factor; lanes stay usable ~10-20 s; a BLOCKED cell holds nothing (cleared every step)); the `Nav::PressureField` (fed by the seeded troughs, plus the optional unit injections above) is a SIGNED scalar field (positive = jams/crowd, negative = seeded lanes) that DIFFUSES every frame (`PressureField::update`: one Jacobi step read→write that resolves its 5 raster chunks + 4 neighbour chunks ONCE PER CHUNK (neighbours are then plain array indices, and a chunk whose own and whose neighbours' cells are all quiet is skipped outright), "Nav/Pressure diffusion" (dt-scaled, clamped to 0.25) and a "Nav/Pressure half-life" of 6 s (the seeded trough has to outlive the walk it was planned for) — same frame-rate-independent half-life form as the flow; a "Nav/Pressure floor" (0 = off) below which a neighbour is too faint to diffuse at all (note a trough that spreads without paying for it grows until the half-life catches up, and a flat field has no gradient to steer by); walls reflective — in the diffusion AND in `gradient(pos, raster)`, which substitutes the centre value for a blocked neighbour: a wall cell holds 0, and against a negative seeded trough a plain read makes it look like a HIGH-pressure spot that pushes everything away from every wall; chunks grow where pressure reaches a border cell, evict once quiet; ONE current buffer units read and inject into) and PUSHES THE FLOW (the step hands each active cell's gradient to a callback that splats −∇p·gain into the lanes — one traversal, no second pass re-resolving neighbours through the hash, where gain = 10^"Nav/Pressure flow gain 10^x" — the tweak is the EXPONENT, so one slider spans 0.01 .. 1000 with fine control at the low end). Nav tweaks: Enabled, Field radius 250, Rebuild interval 0.25, Clearance cost 1, Chunk keep frames 120, Flow half-life, Flow max (per-cell magnitude cap — splats SUM, so a milling crowd could out-shout a seeded lane), Seed area/cooldown/max-per-frame/range/trough/trough-squeeze, Pressure diffusion/half-life/floor/flow gain (10^x), Debug draw 0..2 = off / chunk outlines + descent arrows of "Debug team" within "Debug radius" of the player / crowd FLOW arrows (green→white and longer with strength, scaled against "Nav/Flow max" so a reading means the same thing everywhere; hidden below "Nav/Debug flow min" 0.5 m/s so the haze does not bury the lanes) + PRESSURE columns (jams yellow→red rising, seeded troughs blue dipping; diamond + bar sized by value) of "Debug team" ("Debug team" 8 = the player's goal field). Server/single-player only (units gate on authority; clients feed obstacles only). The Nav library ALSO offers per-walker helpers the PLAYER uses (`lineOfSight(a,b,radius)`, `steerPoint` string pulling, `avoid` raster whiskers, `chooseSide`)

## Profiling
* CPU + GPU scope profiler, part of Core: `Core.Profiler` (`Globals::profiler`), re-exported from `Core.ixx` (its interface must not import Core back — textual std includes in the GMF)
* `ProfileScope scope("Name", EProfileCategory::Physics);` — RAII, name MUST be a string literal. ~15ns hot path: two rdtsc + one 32-byte store into the calling thread's lock-free single-writer ring (`ProfileTrack`, 32K records/1MiB, overwrite-on-wrap). Categories are the `EProfileCategory` enum + two POSITIONAL tables (name, color) in Profiler.ixx — add all three together (`Game` = amber, the Game library's main-thread ticks: "Game update"/"Game windowed"/"Structures authority" + its sub-scopes/"Npc service"/"Player ..."; the Nav WORKER spans use it too — "Nav build" (one per navFieldBuild job), "Nav raster blocked"/"Nav raster clearance" (inside the build), "Nav flow step"/"Nav pressure step" (one per fan-out callback, covering that callback's grain of chunks — per-chunk scopes would flood the rings)). The entity pass carries a scope for OPT-IN entities (`EEntityFlag_Profiled`, `setProfiled()` one-way latch: Animator/Force/Script/GameUnit set it at spawn, machine structures — barracks/turret — latch it in update since machineKind is stamped post-spawn; static scenery stays scope-free so it cannot flood the rings), named by `Entity::name` — the entity's ONLY name storage, an INTERNED pointer (`Profiler::internName`: permanent mutex-guarded dedup pool; records store names BY POINTER and outlive entities, so an owned string would dangle — set by `setName`, null/"Entity" when unnamed; the entity owns no name buffer at all))
* Thread registration is EXPLICIT-ONLY: every profiling thread calls `registerThread(name, sortKey)` at startup (main registers as "Main" in the profiler's static-init ctor; workers in `workerMain` with `SORT_KEY_WORKER + index`). A ProfileScope on an unregistered thread debug-asserts and no-ops. Registered: Main, JobWorkerNN, GPU, TexStream/MeshStreamer, V3 Loader, JobTimer. Deliberately unregistered: transient startup pools + stress-test threads (tracks are permanent, 1 MiB each) and miniaudio's thread
* Scopes MAY span a JobSystem `wait()`: the open-scope stack MIGRATES with the parking fiber (`suspendScopes` at both park sites closes on-thread segments; `resumeScopes` replays on the resuming thread). Timing = sum of on-thread segments (parked time not counted); memory attribution follows the fiber. `ProfileScope` caches NO track pointer. Max migrating depth 32
* Records push at scope END (rings ordered by end time). `Profiler::endFrame()` (main loop, after present) pushes a frame mark (512-frame ring) and re-anchors rdtsc↔QPC calibration. The profiler self-initializes at static init; `endStaticInit()` FIRST THING in main() closes the synthetic "Static init" scope (static-init allocations attribute there). Recording is always on; the panel's PAUSE freezes the data, not the bookkeeping (`setPaused`: ring pushes drop, open/close keeps running so migration + Memory attribution stay live; resume lays a GAP pseudo-frame, `isFrameGap`)
* GPU pass timings: `GpuProfiler` (RendererVK Objects/, member of Renderer) writes timestamp pairs in the primary CB around each pass (outside render passes only — multiview would replicate); one query pool per frame slot, results read in beginFrame after the fence, pushed into a named "GPU" track. GPU↔CPU alignment via VK_KHR_calibrated_timestamps, fallback submit-tick anchoring
* UI "Profiler" panel (`UI:ProfilerPanel`): frame bar graph (click = pause + inspect; Space = pause), per-track flame graphs (zoom/pan/fit), sortable stats table, and an UNCAPPED fps readout in the toolbar = max("main loop", "GPU Frame") of the displayed frame (what the frame would run at without the vsync/fence throttle — the main loop scope EXCLUDES the fence wait by design; per track it takes the depth-0 record with the largest overlap with the frame window and uses its full duration, since the GPU frame straddles the marks; live = 0.9/0.1 EMA fed once per displayed frame, paused = exact; tagged CPU-/GPU-bound). Live view shows frameCount-3 so CPU and GPU data are complete

## Memory
* Allocation tracking + treemap, part of Core: `Core.MemoryTracker` (NOT re-exported from Core.ixx; its only consumer is the UI Memory panel). `Globals::memoryTracker` attributes every allocation to the PROFILE-SCOPE PATH open on the allocating thread: allocator hooks (`g_memoryAllocHook`/`g_memoryFreeHook` in Core.Allocator; alloc fires after allocation, free before release) walk/build a `MemScopeNode` tree (4096-node pool); live pointers in a 64-shard open-addressing map (preallocated)
* Construction via `#pragma init_seg`: allocator `.CRT$XCA` → profiler `XCA1` → tracker `XCA3` — static-init allocations are tracked. The hooks NEVER allocate (everything preallocated in the tracker ctor); pools deliberately leak at exit (dtor only uninstalls hooks — later global dtors still free tracked memory). `MemoryHookSuppress` (per-thread RAII) mutes hooks for PERMANENT infrastructure allocations that would misattribute — never around blocks that get freed. Unregistered threads land in "<other threads>"; overflow counts into `getDroppedAllocs` (red in the panel). Node identity is the name-literal pointer
* UI "Memory" panel (`UI:MemoryPanel`): squarified treemap — one box per scope path, area ∝ bytes, nested, colored by category; a path's own bytes are the uncovered parent background. Click zooms; tooltip shows live/self/churn; "Cumulative" toggle sizes by total-allocated-since-startup (churn finder)

## Threading
* Fiber-based work-stealing job scheduler. Jobs run ON Win32 fibers, so `wait()` inside a job PARKS the fiber and frees the worker; the fiber may resume on a different thread — hence `/GT` everywhere: never cache `thread_local` addresses (or `PerWorker::local()`) across a wait
* `Globals::jobSystem`: workers default to (cores − 1, the 1 being the MAIN THREAD — it registers as a helper context and runs jobs inside `wait()`, so workers + main exactly covers the count), where WHICH core count is used flips with machine size at `c_logicalWorkerCoreMax` (16) PHYSICAL cores (`RelationProcessorCore` topology; fallback logical/2 if the query fails; `JobSystemDesc::numWorkers` overrides): at or below it the LOGICAL processor count (a narrow machine has too few cores to fill a parallelFor, so the second thread per core is worth having even at half efficiency), above it PHYSICAL only (hyper-threads share a core's execution units, and a wide machine already holds the frame's parallel work — logical sizing there only buys scheduling pressure and cache contention). So 6c/12t → 11 workers, 12c/24t → 23, 24c/48t → 23. `initialize()` MUST run on the main thread (registers it as helper: main-thread `wait()` runs jobs); shutdown is ~JobSystem at init_seg XCU5 (idempotent `shutdown()`; atexit runs on the main thread — see Style for the teardown scheme). `submit(callable, JobProfile, priority, &counter)` / `submitDelayed` (timer thread) / `wait(counter)` / `parallelFor(begin, end, grain, JobProfile, fn)` (shared cursor, caller participates, nestable). The `JobProfile{ name, EProfileCategory }` is REQUIRED (name = string literal, asserted): execute() opens that ProfileScope around every job body — and parallelFor around each participant's whole run — so a worker can NEVER run something invisible to the profiler; job bodies open finer scopes inside it, never a duplicate of the job's own name. Callables store INLINE (`Job::StorageSize` 64, `sizeof(Job)` 128, both static_asserted — capture a pointer to bigger state). Counters/data captured by reference must outlive the wait
* Scheduling: per-context Chase-Lev steal deque (`Threading:StealDeque`) + 3 shared Vyukov MPMC priority rings (`Threading:MPMCQueue`) + resume queue + free-fiber/job pools — all preallocated, hot paths lock-free, zero runtime allocation. A worker's own continuations go to its LOCAL deque regardless of priority (fork/join locality). Idle workers spin then sleep on an eventcount. On pool/queue exhaustion submits execute INLINE with a debug assert — raise `JobSystemDesc` capacities
* `Threading:JobSync`: `JobMutex` (fiber-parking mutex — use for long exclusive sections like V3 inference so waits hold a fiber, not a worker), `JobEvent` (manual-reset, idempotent signal), `PerWorker<T>` (cacheline-aligned per-context slots, `.local()`/`.forEach()` — the substrate for per-worker queues/dirty lists)
* Job cost heuristic: graph nodes are wall-time EMA'd (timing is opt-out via `EJobFlag_Untimed`; parked waits count); each `run()` recomputes critical-path ranks and promotes top-rank Normal jobs to High. `parallelFor(begin, end, JobCost&, JobProfile, fn)` auto-grains toward `parallelForTargetChunkNs` (25µs) with per-call feedback. Ad-hoc submits are deliberately untimed
* `JobGraph` + `JobResource` (`Threading:JobGraph`): declared-access DAG — `addJob(name, fn).reads(a).writes(b).after(id)` derives hazard edges render-graph-style in declaration order; edges always point earlier→later so cycles are impossible. `compile()` once, `run()`/`wait()` per frame, zero allocation, re-runnable. `JobResource` is a process-unique id created next to the data it names
* Stress harness (`Globals::jobSystemStress`, Threading/Stress tweaks): throughput/parallelFor/hazard-checked-graph/fiber benchmarks + "Run self test". The way to re-verify changes here is a temp console torture target
* Invariants (each guards a race that was actually hit): a waiter may FREE the counter the instant it observes zero, so `isDone` is "the FULL value is zero"; fiber-wait registrations ride the high 8 bits (`JobCounter::WaiterInc`) keeping the value nonzero; the zero transition claims the wait list with ONE exchange of a taken-sentinel; the only post-zero touch is an address-only `notify_all`. A parked fiber's wait node lives on its stack (signaler spins on `switchDone`); `runFiber` reads fiber state BEFORE publishing `switchDone`; recycling freelists are LIFO tagged-index stacks, NOT rings (a preempted ring pop fakes full/empty — this exhausted the job pool in practice); inline helping in `wait()` is depth-capped. Don't hold a fiber wait across `shutdown()` (asserted)

## Procedural
* The procedural world layer: diffusion terrain, FFT ocean, scattering, terrain physics, and the camera-centered bakes they share. `Globals::terrain` (TerrainStreamer), `Globals::terrainCollider` (TerrainCollider), `Globals::ocean` (OceanGenerator), `Globals::scatter` (ScatterSystem) — all in init_seg `OC_SEG_PROCEDURAL`, the first globals to destruct (their dtors free renderer residency / collider bodies and may wait on in-flight jobs; order among them is undefined and deliberately independent). main.cpp updates all four per frame in order terrain → collider → ocean → scatter (after entity updates, before present). `ocean.swellTravelAngle()` → `terrain.setFlowWindAngle` is wired once at init
* `ITerrainSampler` (`:TerrainSampler`): THE terrain field interface — pure functions of world (x,z), seeded/thread-safe/world-continuous, held as `shared_ptr<const>` (workers finish against old maps across rebuilds). `samplePoint`/`sampleGrid` fill a `TerrainPoint` (height, water level, temperature + sea-level baseline, humidity, fog, flow angle) in ONE evaluation — wide-area consumers must use `sampleGrid` (hoists per-point overhead; for V3, one lock per tile). `ESampleDetail::Coarse` for km-scale bakes. One `lapseRatePerMetre()` for the whole world
* `TerrainGenV3` (`:GeneratorV3`, `Private/Diffusion/`): ONNX diffusion pipeline (ONNX Runtime + DirectML, 2.28 GB models loaded once per process, reseeded never reloaded; the load is DEFERRED until "Terrain/Enabled" — `rebuildMaps` early-outs disabled without constructing the generator, and constructing it is the only load kick. No unload API: disabling after a load keeps the models resident). 256x256 tiles (7.68 km at native 30 m/px; `metersPerPixel` is a uniform world scale); a point read is a bilinear tile fetch; a cold tile ~1.5 s with inference serialized by a fiber-parking JobMutex; concurrent requesters park on a per-tile JobEvent. Tiles cache to disk under `Assets/Local/Diffusion/<seed>/` (zstd). Sub-30 m relief is slope-masked noise (`:Noise` fbmEroded). `isReady()` gates everything until models load (else a flat world bakes into caches)
* `TerrainStreamer` ("Terrain*" tweaks): render-only chunk streaming — bounded LOD ring around the camera, generated on up to "Gen jobs" self-continuing Low-priority pump jobs (`kickPump(n)` CAS-claim + exit-recheck protocol, nearest-first; `createMeshScene` runs IN the pump, main only initializes the container; warm-tile mesh builds overlap cold V3 waits since those park fibers). Chunks leaving the ring free all GPU residency (`~ObjectContainer`). Owns THE world datum other systems consume, handed out not duplicated: `seaLevel()`, `activeWaterReach()`, `activeFlowField()`, `activeClimateMaps()`, `activeTerrainData()`. Also bakes the shared TERRAIN-DATA MAP (2 near/far cascades, RGBA32F: height, water level, packed fog|flow|temp|humidity → `Renderer::setFogTerrainHeightMap`; consumed by fog terrain-follow, ocean depth/level (GPU via terrain_height.inc.glsl + CPU copy for buoyancy/wind steering), terrain coloring, the terrain sun march) and the splat textures (BC-baked to Assets/Local/TerrainTex at startup)
* `HeightMapBaker` + `applyWaterReach` + `applyFlowField` (`:HeightMapBaker`): the async snapshot bake driver — one Low job in flight (JobCounter-polled), re-bakes on camera drift > range/4 or config change (configs carry `operator==` so tweaks stay live), center snapped to the coarsest texel lattice. WaterReach: chamfer distance-to-ocean sinks the baked water level under ground the ocean can't reach (keeps beaches off inland hollows). FlowField: 8-bit per-texel flow direction — toward land through the surf zone, downhill elsewhere; baked into both maps' flow bits
* `OceanGenerator` ("Ocean*" tweaks): CPU side of the FFT/Tessendorf ocean — camera-following geometry clipmap (fixed cell size per ring, CDLOD morph; a FLAT "Horizon band" ring extends to the far plane so the sea always meets the horizon) split into SECTORS (each an ObjectContainer/RenderNode + SpatialEntry on `SpatialLayer_Terrain`, one shared snapped transform, duplicated border verts): the Spatial Main gate skips off-screen sectors (ocean is PASS_MAIN only) and a "Dry sector cull" skips sectors fully buried AND inside the streamed-mesh radius. The horizon band is exempt from BOTH under-terrain culls (negated cell size flags its vertices — its huge triangles break the vertex cull's footprint assumption). The simulation (spectrum → IFFT → displacement/gradient maps, re-evaluated every frame so every param is live) is `OceanSimulationPipeline` in RendererVK. Bakes NOTHING itself — shoaling/surf/swash/land cull all read the streamer's terrain-data map. `sampleWaterHeight` = CPU buoyancy field from the GPU displacement readback (App wires it into `PhysicsWorld::setWaterSurface`); its `sampleDisplacement` is a full CPU MIRROR of `oceanSampleDisplacement` (ocean_wave.inc.glsl) — the shader is what you see, the mirror is what floats on it: changing either without the other is a silent bug (bodies sink through drawn waves / float on dry sand). Returns -FLT_MAX only past the run-up band; inside it the drawdown floor sinks the surface under the sand (beaches bodies). WIND: the dominant spectrum term travels AGAINST `windDirection` — swell heading = `swellTravelAngle()` = wind + π; `steeredWindAngle` slews the SIM wind toward the baked shore flow so waves roll inland (per-pixel domain rotation is disabled in ocean_wave.inc.glsl — creases; see the comment before reviving). "Ocean/RT" tweaks budget per-pixel scene rays
* `TerrainCollider` ("Terrain/Collision" tweaks): camera-centered ring of small static triangle-mesh collider tiles (default 32 m tiles within 96 m, 1 m spacing = the render LOD0 lattice, so bodies rest on the drawn surface). Never touches the render chunks: each tile is `sampleGrid`'d + BVH-built (`createCollisionMesh`, standalone/thread-safe) on ONE in-flight Low job, nearest-first; main thread only creates/destroys the static bodies (layer "Terrain"). Tiles clear on sampler identity change; half-tile eviction hysteresis. Verify with Draw colliders
* `ScatterSystem` ("Scatter" tweaks): trees/rocks/grass — config is two function-local tables in Scattering.cpp (`scatterAssets()`: .oc models with footprint/scale/slope settings; `scatterRules()`: climate ATTRACTORS in real units (°C, mm/yr) with Gaussian falloff — no biome enum). Cell placements computed deterministically on pump jobs (same claim protocol, "Scatter/Gen jobs") from the SAME sampler the terrain renders (footprint dart-throw, cluster noise, slope/altitude bands); per-rule `viewDistance` spawns/despawns instance groups without regenerating cells; everything regenerates on sampler identity change

## Network
* Winsock UDP/TCP + reliable-UDP game protocol (Core-only; Ws2_32 + Bcrypt PRIVATE). Partitions `:Address`, `:Serialization`, `:Socket`, `:Crypto` (internal), `:Reliable`, re-exported from `Public/Network.ixx`. IPv4 only; raw little-endian wire format, no versioning — both ends must match (`protocolId`). `NetHost` is strictly single-threaded, main-thread-only
* `:Serialization`: `NetWriter`/`NetReader` over caller-provided spans — trivially-copyable `write<T>`/`read<T>`, varint (LEB128 + zigzag), strings, unorm/snorm/`writeQuantized`; overflow-safe (`overflowed()`), reads are zero-copy views
* `:Socket`: non-blocking RAII — `UdpSocket` (big SO_RCVBUF, SIO_UDP_CONNRESET disabled), `TcpSocket`/`TcpListener`, `TcpMessageStream` (length-framed), `netResolveHost`. Winsock initialized lazily
* `:Reliable`: `NetHost` — symmetric client/server/p2p over one UdpSocket (simultaneous handshake tie-broken on connect salts). Poll model: `send(peer, bytes, ENetDelivery, channel)`, per-frame `update(dt)` (batches into ≤maxPacketSize packets), drain `takeEvents()`. Deliveries: Unreliable, UnreliableSequenced, Reliable (ordered, ack bitfield piggybacked on every packet, RTT retransmit, transparent fragmentation) on 8 channels (channel rides the kind byte, zero wire cost — bulk transfers don't head-of-line-block). 4-way anti-spoof challenge handshake (responder stateless until the challenge round-trips); `NetHostConfig::encrypt` adds ephemeral ECDH P-256 + AES-128-GCM per payload packet (+16B; Windows CNG, hardware AES) — secure against spoofing/eavesdropping/tampering, not active MITM. Keepalives/timeouts, per-peer RTT/loss stats, live link sim (`simPacketLoss`/`simLatencyMs`/`simJitterMs`)
* ABUSE LIMITS (`NetHostConfig`, enforced before any parsing/allocation): per-source-address token bucket in a FIXED 512-slot hash table (a per-address map would itself be the memory attack; a foreign slot is only stolen once its owner has refilled — else a flooder evicts active peers; an UNUSED bucket must be claimable on the first packet); `maxPacketsPerUpdate` bounds one update globally; `maxPeersPerIp`; `maxQueuedReliablePerChannel` disconnects never-acking peers (`ENetDisconnectReason::Overflow`, latched and acted on in update() — never free a peer inside send()). `NetHostStats::packetsDroppedPerSec` surfaces rate limiting (server title bar)

## NetFuzz
* `Code/NetFuzz` (console exe, Core + Network; VS target `run-netfuzz`): protocol fuzzer, the regression gate for wire-format changes. `NetFuzz reader|host|game|all [iterations] [seed]` — deterministic seed printed
* `reader`: any view NetReader returns must lie inside the input buffer. `host`: drives a real in-process NetHost (cold-start, pre-auth garbage, authenticated payloads via real handshake; also smoke-tests the encrypted path). `game <ip[:port]>`: the only mode reaching NetworkManager's handlers — hostile client vs a live `App --server --headless` run with `--no-encrypt` (it speaks the handshake by hand and mirrors `GameProtocolId`)
* Harness constraints that silently neuter it if broken: pump the in-process server between handshake polls, drain the socket first, raise the rate limits for the fuzz phases

## Audio
* miniaudio (device, mixing, decoding; `MINIAUDIO_IMPLEMENTATION` in Audio.cpp) + Steam Audio (HRTF binaural, static phonon(d).lib), wrapped in `module Audio`; neither API leaks (opaque handles)
* `Globals::audio`: `initialize()` opens the default device (48kHz stereo) + Steam Audio context/HRTF; `update(camera[, listenerVelocity])` once per frame — recomputes HRTF direction, distance attenuation, doppler on the game thread (audio thread reads atomics). Master volume under Audio/System
* Mono sounds route ma_sound → per-source `BinauralNode` (256-frame `iplBinauralEffectApply` blocks behind FIFOs) → endpoint; stereo or `setRelative(true)` plays unspatialized. Only the HRTF direct path is used so far
* `AudioBuffer`/`AudioSource`: RAII handles. `loadSound(path)` (WAV/FLAC/MP3) or `createBuffer(format, pcm, rate)` for procedural sounds; buffers must outlive sources playing them. `playOneShot(buffer, pos, gain, pitch)` / `playOneShot2D` — 32-source pool, steals oldest
* Gameplay integration lives in Entity: `AudioComponent` + the "Trigger Audio" node (dynamic exec pin per alias, special-cased in NodeDef.ixx/Scene.cpp; connected Position/Volume/Pitch pins override via `ctx->entityTriggerAudio`'s overrideMask) and "Stop Audio"

## Animation
* Skeletal animation runtime (Core-only). Partitions: `Animation:Skeleton`, `Animation:Clip` (`AnimationClip`/`AnimationSet` + `AnimationPlayer`: CPU sampler producing bone palettes, crossfades, `BlendSpace1D`, per-bone posing via `setBoneTransform`/`setBoneOffset`), `Animation:StateMachine` (`AnimStateMachine`: parameter-driven states/transitions)
* File builds Skeleton/clips from Assimp; RendererVK consumes the palette for GPU skinning; Entity's AnimatorComponent + .apl assets drive it (World caches retargeted clip sets per skeleton+animator)

## File
* Asset + filesystem functionality. `ISceneData::createAssimpLoader()` for model files, `createProceduralLoader()` for generated geometry (terrain, skysphere, debug shapes)
* Cooked scene cache: `ISceneData::loadCached(path, mergeNodes, preTransform, SceneCookOptions)` (World uses it for every non-procedural container) serves a binary snapshot (`Assets/Local/Cooked/<stem>_<hash>.vsc`; `File:CookedSceneData` reader, SceneCooker.cpp writer) validated by magic/version, source mtime+size, options hash, per-converted-texture stamps — any mismatch re-imports and re-cooks in place. Cooking bakes meshopt LOD chains (so changing LOD tweaks recooks on next start) and converts non-DDS textures (loose AND embedded) to BC1/BC3(alpha)/BC5(normals) .dds so they mip-stream (alpha-masked diffuse mips alpha-coverage-rescaled). Skinned/animated scenes always bypass the cache. Delete `Assets/Local/Cooked/` to force a recook
* `AssetParser` (`File:AssetParser`): the text format behind all engine text assets — indentation-defined hierarchy of `AssetNode` (key + values + children), `#`/`//` comments, quoted strings, comma vectors; `loadAssetFile`/`parseAssetText`/`writeAssetText`, case-insensitive `find`, typed accessors. `File:ObjectDescription` parses .oc into `ObjectContainerDesc` (`loadObjectContainerDesc`) — lives here so Procedural's scatter assets import through .oc too
* `FileSystem` (`File:FileSystem`) is THE disk seam — the ONLY code that includes `<filesystem>`/`<fstream>` (Core stopped exporting them). Plain UTF-8 `std::string` paths, no `std::filesystem::path` in any consumer. API: reads/writes (`readFileStr`/`readFileBytes`/`writeFileStr`/`writeFileBytes`), queries+mutations (`exists`/`isDirectory`/`isRegularFile`/`fileSize`/`lastWriteTimeSec`/`copyLastWriteTime`/`createDirectories`/`remove`/`removeAll`/`rename`/`copyFile`/`listDirectory`(+`Recursive`, returns `DirEntry{path,name,extension,isDirectory,size}`)/`currentPath`/`setCurrentPath`/`absolutePath`/`canonicalPath`/`weaklyCanonicalPath`/`relativePath`) and pure PATH MATH that touches no disk and carries no thread rule (`join`/`parentPath`/`filename`/`stem`/`extension`/`replaceExtension`/`normalize`/`isAbsolute`/`pathEquals`)
* MAIN-THREAD IO ASSERT: every FileSystem IO call asserts when it runs on the main thread (the thread that called `initialize()`) — disk latency there is a frame hitch. Intentional main-thread IO says so, either with the RAII `FileSystem::AllowMainThreadIO` scope (thread_local, nestable; do NOT hold one across a fiber wait — /GT) or the trailing `allowMainThread` flag per call. Every FileSystem IO function also carries a profile marker named after it ("FileSystem::readFileStr", ...) in the File category — via a local `IoScope`, NOT a plain ProfileScope: IO runs on threads the profiler deliberately leaves unregistered (the transient startup texture-bake pool), where ProfileScope would dereference a null track, so the marker no-ops when `threadTrack()` is null. `FileSystem::assertIoThread(allow)` is the same main-thread check, exposed so File's OWN internals (CookedSceneData/SceneCooker stream binary ranges through `FILE*`/ifstream, which no read-it-all API can express) declare their policy too. Already marked as main-thread-by-design: main()'s whole INIT phase (one scope, released at `initScope.stop()` — so anything the FRAME LOOP does trips the assert), `loadAssetFile` (prefab/.oc text, read once per asset at first spawn then cached), scene load/cook, shader compiles, ScriptHost compile/load, the editor panels (explicit user actions), game save/load
* `FileSystem::initialize()` sets the working directory to `Assets/` and records the main thread

## UI
* ImGui (docking branch) for all UI. `UI::update` runs the panels; panel mutations flow back as an `EntityChange` queue (`takeEntityChanges()`) — the UI never mutates the world directly
* TWO-PHASE UI FRAME: ImGui is single-context/single-thread, so the widget pass (`UI::update`) stays serial, but the data-heavy panels split into `prepare()` (NO ImGui — worker-safe data work) + `render()` (ImGui, reads what prepare built). `UI::prepare()` (main.cpp: right after `input.update`, BEFORE controls/camera/game.updateWindowed) submits one High job per data-heavy panel that was OPEN last frame (`m_profilerOpen/m_memoryOpen/m_logOpen` = the `ImGui::Begin` results; a hidden panel costs nothing) on `m_prepareCounter`; `update()` waits on it first thing ("UI prepare wait" scope — a visible wait means the prep did not fully overlap). Every panel prepares INLINE in its render if nothing ran ahead, so the split is an optimization never a requirement. Prepared panels: AssetBrowser (its listings are CACHED per directory — it used to enumerate the filesystem EVERY FRAME: the current folder plus a `file_size` syscall per list row, and the tree ran TWO `directory_iterator` passes per expanded folder recursively, ~1 ms/frame of syscalls. Now `listing(dir)` serves a cached `DirListing` (sorted dirs-first, sizes included), scanned inline the first time, re-scanned by the prepare job at most every `RescanIntervalSec` (1 s — files also arrive from outside: prefab saves, compiled scripts, cooked assets) and ONLY while the panel is focused/hovered (`m_active`) and only for listings a render actually read (`touchedFrame`); `invalidateListings()` on any mutation the browser itself makes (new script/text/prefab, rename, prefab save) forces the next draw to re-scan, and `navigateTo` re-scans the entered folder on the spot), ProfilerPanel (auto-pause check + ring snapshot — `snapshotTracks` is itself a `parallelFor` over the tracks (per-track copy+sort, record buffers handed back to per-track scratch slots to keep capacity; the `m_trackMaxDepth` map merge stays serial) + `aggregateStats` when the Stats tab was open (`m_statsVisible`); `selectFrame` from a worker only ever SETS paused — an atomic exchange — resume stays in render), MemoryPanel (`buildSnapshot` — the tracker's atomic tree is reader-safe), OutputLog (log snapshot under its mutex + level/text filter into `m_visible`). CONTRACT: prepare reads only view state render wrote LAST frame (pause, zoom, filters, metric toggle) — the same ordering the single-threaded version had, so a filter edit shows next frame; a panel that fiber-waits inside prepare must not cache thread_locals across it
* Panels:
	* Viewport — renderer output; assets drag-drop from Content to spawn at the cursor (`Camera::screenToWorld`)
	* Scene (`SceneView`) — live hierarchy. Selection is an OWNING `EntityPtr` (a raw one dangled when anything else deleted the entity); `pruneSelection` drops it once its tree leaves the world. F2 rename, Delete, drag-reparent, drag-to-Content saves a prefab, prefab instances locked (unpackable)
	* Properties — name/enabled/transform, render info + bounds toggle, script path, live public ScriptData fields
	* Content (`AssetBrowser`) — filesystem browser rooted at Assets/; drag into viewport/hierarchy, prefab drop guards. THE place scripts are created and opened: "New Script" writes a minimal .dsl and opens it; double-click/drag a .dsl reaches the Script Editor (whose sidebar holds Save/Reload — it has no path field of its own)
	* Script Editor — the DSL text editor (autocomplete-driven); Text Editor — plain text
	* Log (`OutputLog`) — `Core.Log` view with filters/search
	* Entity Editor (`EntityEditor`) — authors a .pre document: name/transform + one section per component, bound to draft SpawnInfos. Changes queue a `RespawnEntity` (`commitRespawn`) since most SpawnInfo fields are spawn-only; drag widgets use `commitDrag(...)` which also calls `applyDraftsLive()` per change frame (viewport tracks the drag, one respawn on release). `applyDraftsLive` only touches what's re-appliable in place (RenderComponent::localTransform, ForceEmitter setters, LightComponent's list) — physics/animator/script data stay respawn-only
	* Tweaks (auto-populated), Stats (`ui.setRenderStats`), Profiler, Memory
* Gizmo split: `UI.Gizmo` declares `EGizmoMode` + `IGizmo`; Input's GizmoController implements it; main owns the object and does `ui.setGizmo(&gizmo)`. `UI::update` drives it after panels settle viewport rect + selection; main calls `ui.drawGizmoEntity(renderer, dt)` at the entity-update point

## Input
* SDL3 event pump; listener objects with std::function callbacks. `addKeyboardListener()`/`addMouseListener()`/`addSystemEventListener()` return move-only RAII handles (`InputListenerHandle<T>`, InputDef.ixx) that unregister in their destructor — assign callback slots through `->`; no manual remove API
* `GizmoController` (in Input because it needs mouse listeners + viewport focus; DRIVEN by the UI via `IGizmo`): spawns `Entities/Gizmo.pre`, follows the Scene selection, constant apparent screen size, T/R/G switches Translate/Rotate/Scale, drag writes back to the entity (a kinematic/static physics body does NOT follow — see PhysicsComponent)
* `VrInput` (`Globals::vrInput`): OpenXR controller input via `Core.VrSession`'s `IVrSession`
* Camera controllers: `FreeFlyCameraController` (WASD+mouse) and `VRFreeFlyCameraController`; App picks by `renderer.isVrEnabled()`

## Testbed keys (InputControls)
* F5 reload shaders, F6 recompile script; T/R/G gizmo modes
* 1 clear test lights, 2-6 spawn one light of each type at the camera, 7 burst 100 points (all share one lazily built template; values live on the component, deliberately not serialized). While the game HUD's hotbar is ACTIVE (see Core.GameHud) keys 1..9,0 select hotbar slots instead and the testbed meanings above are skipped
* 8/9 spawn+throw a physicsCube/physicsSphere; 0 hangs an 8-cube joint chain from `staticBody()`
* N spawns a force emitter from the Force/Emitter tweak values; M re-applies those tweaks to the LAST spawned emitter; H 500-emitter stress burst; F physics sphere carrying a bubble; J territory query grid; B clear
* P toggles GI probe debug, O cycles its mode, L aims the sun down the camera forward
* C toggles player control: on a network client it drives the locally-owned capsule (see Multiplayer) with the same first/third-person camera follow; single player / server it spawns + possesses a local upright player capsule (playerCapsule.pre, `LockRotation`) — camera-relative WASD velocity steering, Space jump off a ground raycast, LShift sprint, V toggles first/third person (third pulls in on a wall raycast), camera follows the interpolated body pose ("Player" tweaks: eye height/third distance/sprint; move params shared with "Network/Player"). `InputControls::applyPlayerCamera` overrides the frame camera right after the fly camera in main
* Network: U (server) throws netPhysCube.pre, C (client) toggles player control, K fires "NetPing"

# Asset files
All text formats parsed via `AssetParser`, registered by `AssetRegistry`:
* `.oc` (ObjectContainer): `ObjectContainer <name>` + `Path`, optional `Loader Procedural`, `MergeNodes`, `PreTransformVertices`, `DecimationFactor` (<1 meshopt-simplifies at cook; cache-only, so skinned/procedural containers never decimate), `MaterialOverrides` (per-material `PipelineIdx <pipelineName>`, `ExcludeFromRayTracing`, `UseSceneTextures`, `DiffuseTexIdx`/`NormalTexIdx`/`MetalRoughnessTexIdx`). Skinned containers MUST keep `PreTransformVertices false`
* `.pre` (Prefab): `Prefab <name>` root with `Component Render/Animator/Script/Physics/Audio/Particle/Force/Light/Network/Scene` children; `Component Scene` nests child `Prefab` references. Render requires `ObjectContainer <name>` + `Node <path>`, `Type StaticMesh|SkinnedMesh` (skinned adds a nested `Rig <name>`), plus `Position/Rotation/Scale`; entity-level `Enabled false` authors the disabled state. Spawn by prefab name (`world.spawn`) or path (`world.spawnAssetFile`)
* `.anm`: `Animation <name>` clips — source `ObjectContainer`, `Loop`, `Skip` (channels), `Event <name> <normalizedTime>` notifies (fired into the entity's script/animator `onEvent`)
* `.apl`: `Animator <name>` graph — `Parameter`s (Float/Bool/Trigger), `Clip <name>` + `Anim <clipName>` bindings, `BlendSpace1D` with `Sample`s, `StateMachine` (`Entry <state>`, states with `Play`/`SpeedParam`/`SpeedScale`, `Transition`/`AnyTransition` with `Condition`/`ExitTime`/`Fade`)
* `.scr`: the removed node editor's format (generated C++ + `//@graph` metadata) — legacy reference only: reads as plain text in Content, never cooked, no editor. Author `.dsl` via DslCompiler instead
* `Assets/Local/` is generated output (SPIR-V + shader dumps, compiled script DLLs/PDBs, cooked scenes `Cooked/*.vsc` + converted .dds, `TerrainTex/` splats, `Diffusion/` tile cache) — never hand-edit


# Game
* The actual game built on the engine (`Code/Game`, static lib linked by App). Top-down tactical PVP whitebox (PvE — ambient field, waves, camps, guards, player combat — is REMOVED): place Force emitter pylons + economy to siege the other team. `App.exe --game` = single player sandbox; MULTIPLAYER = `--game --server` (WINDOWED listen server — GPU readbacks drive the sim, so `--headless` refuses `--game`) + `--game --connect <ip>`. Server runs ALL sim; sync is three-layered: (1) player-structure MIRROR over game events — GPl/GRm/GCb reliable on change + join replay (idempotent mirror* appliers in StructureSystem, notified via its onStructure*/onCableChanged hooks) + GSt at 5 Hz (u8-quantized health/charge/fuel/minerals/outputFrac/connector-utilization/powered for the first 79 structures + resource totals; mirrored emitters drive their LOCAL ForceComponent from the synced outputFrac, so client-side fields are real and the client player's shield readbacks work); (2) units/shots replicate as network entities (`Component Network` in those prefabs + player.pre); (3) each client ADOPTS its server-spawned capsule (`GamePlayer::clientAdopt`, LocalOwner scan) and owner-simulates through the claim stream, computing shield/health locally. GAME BLOB (replaced the old GSh/GqE shield-mirror events): every snapshot/claim record for an entity WITH a GameUnitComponent carries `NetRecFlag_Game` + 5 bytes (`packGameStateBlob`/`applyGameStateBlob` in NetworkManager.cpp: healthFrac/energyFrac u8, emitter output over a fixed 0..8 range, materialsFrac, flags = collapsed | team<<4). Server-side it is CHANGE-DETECTED (`ServerState::lastSentGameBlob`) and forces the record out even when the pose alone would skip (asleep body / unmoved entity) — a collapse edge flushes on the next snapshot tick. Receivers write the entity's GameUnitComponent + drive its LOCAL ForceComponent output/team, so remote bubbles render true drain/collapse state on the right team and the overhead labels read components directly on every instance; the entity's OWNER keeps its self-computed shield (applyShield off) and takes only server-authoritative materials; claims apply shield-only (never materials/team — client-forgeable). Player capsules are PUPPET GameUnitComponents (`Puppet true` in player.pre: update() is inert, GamePlayer stamps health/energy/collapsed/team each tick and reads materials back as LocalOwner; the server writes m_clientMaterials into the twins); puppets are excluded from turret targeting and NpcSystem clear/save. Client intents = Gq* request events (GqP/GqC/GqD build, GqW routes), gated by a `setEventFilter` (only Gq* ≤64B from clients) and validated by the same authority seams as local input; `NetworkManager::setOnGameEvent` is the C++ dispatch hook. `GameMatch::onClientJoined/Left` (registered from main when game+server) spawn/remove per-client players + replay world state. Gameplay tweaks sync automatically (Core.Tweaks `Synced` flag — all Game/* except Game/Camera). TEAMS: everything is per-TEAM (`GameMaxTeams` 8, but the corridor map spawns one Base per PLAYABLE team, so `GameMatch::PlayableTeams` = 2 is the slot pool — a player without a Base has no respawn anchor, mineral bank or healing). The server = team 0; a joining client gets the LOWEST FREE slot (`allocateClientTeam`, extras double up on the last), stamped onto its capsule's puppet GameUnitComponent + emitter at spawn. That component IS the record: `clientTeam(clientId)` reads it back (−1 = no capsule → the Gq* handlers refuse the request), and the OWNER learns its own team from the snapshot game blob (`applyTeam` deliberately applies outside the `applyShield` gate, so an owner that computes its own shield still receives team + materials; claims never carry either — client-forgeable). NEVER derive a team from the clientId: ids are minted monotonically and never recycled, so a reconnect or a failed first attempt shifted the second connection onto a base-less team (the bug this replaced). GamePlayer follows the component on a client (gated on `hasTarget` so the prefab's authored team is never latched) and stamps it everywhere else. Per-team specifics: players'/projectiles' Force teams set at spawn/adopt (`GamePlayer::setTeam`), structures carry the builder's team (GPl + place requests carry a team byte; fields re-teamed after spawn since prefabs author team 0), cables/demolish validate same-team, minerals/fuel are per-team arrays (GSt sends all 8; HUD/affordability read own team). The map is a walled CORRIDOR ARENA (x ±65, z ±20, ~34 indestructible 10 m borderwall.pre blocks parented UNDER the Ground entity (bodies baked at world pose pre-reparent, pos rewritten to ground-local) + a symmetric node layout — both spawned deterministically on every instance) with a Base at each end (team 0 at (−55,0,0), team 1 at (55,0,0); clients spawn/respawn beside theirs; both invulnerable); `setPlacementBounds` keeps footprints inside the walls. Structure damage: every placeable prefab has `ContactEvents true` — placeStructure installs an onContact hook (enemy-TEAM "Projectile" contacts chip "Projectile structure damage" health) — and tickDamage's rule: hostile = ANY other team's bubble owns the structure's query point (push your field over their buildings to siege). KNOWN gaps: player turrets target only units (not players); GSt caps at 79 structures. SAVE/LOAD (F9 save / F10 load, server/single-player only — clients refuse): full sim snapshot (structures with id/type/pos/facing/node/team/blueprint/stores/route, cables, units; players/shots NOT saved) to `Assets/Local/gamesave.txt` via AssetParser (`StructureSystem::saveTo/loadFrom/clearAllStructures`, `NpcSystem::saveUnits/loadUnits`, `GameMatch::saveGame/loadGame`). loadFrom clears first (removal hooks → GRm prune connected clients) preserving ids, then the server re-broadcasts everything (GPl/GRt/GCb); unit entities resync through normal network despawn/spawn. Without --game the testbed is untouched
* Partitions: `Game:Match` (orchestrator — a STACK LOCAL in main, holds EntityPtrs/Force handles), `Game:Player`, `Game:Structures`, `Game:GameCamera`; barrel `Public/Game.ixx`. main.cpp calls `game.update(dt)` after `networkManager.receive` (pre-physics, direct body setters sanctioned) and `game.updateWindowed(camera, dt)` right after `controls.applyPlayerCamera` (overwrites the frame camera — angled follow cam, LEFT/RIGHT arrows + MIDDLE-drag yaw, wheel zoom; Q/E/WASD are grid hotkeys). In game mode `InputControls::setGameMode(true)` mutes the testbed spawn/possess keys (script-event fires, hotbar routing, F5/F6, T/R/G stay) and the sponza spawn is skipped
* World: flat 400 m plane (`Assets/Entities/Game/*.pre`, baseshapes solid meshes + per-prefab `Color` tints), corridor walls + mirrored resource nodes (see above). NO win condition yet. The engine's Force AMBIENT FIELD support (`ForceSystem::setAmbientField` → `forceAmbientField` in force_field.inc.glsl) still exists but the Game layer no longer calls it (the PvE ambient enemy field was removed; default slope 0 = off)
* Economy: a node produces NOTHING until an extractor is built on it (one per node; the ghost snaps to the nearest free node within "Extractor snap radius", validated again at place time) AND that extractor is fed energy. ENERGY IS A FLOW NETWORK (tickPower): EVERY structure holds LOCAL charge (emitter/extractor/transmitter an "Internal buffer" 10, generators a "Generator buffer" 20, battery "Battery capacity" 100; BARRACKS hold none — they run on conveyor-fed minerals alone, so no energy bar and no power cable attaches); each tick every cable moves energy GRAVITY-FED toward consumers, capped by the cable TYPE's throughput ("Cable throughput" 5/s, "Heavy cable throughput" 20/s): three potential bands — producers (generator/solar) on top always exporting, battery + Connector storage in the middle BALANCING by fill fraction among themselves, consumers below outranking everything until FULL — cross-band links run at full throughput downhill (consumers fill first, surplus banks in batteries), same-band links move a DAMPED fraction (`c_equalizeDamping` 0.25) of the exact equalizing transfer — each source sizes its share without seeing the others, so undamped, N sources feeding one receiver overshoot the balance point together and bounce back next tick (wasted throughput + a flow direction that keeps reversing); damping stays stable up to 1/damping simultaneous sources and removed the need for any direction hysteresis in the draw. Energy still propagates hop by hop, thin lines starve, and a full generator buffer throttles production (export-limited = no fuel burn). Consumers drain their internal battery; empty = unpowered. FUEL IS A FLOW NETWORK TOO (no grids/union-find anywhere; SAME gravity-fed bands — extractor tanks export, fuel tanks/connectors balance, generator/fabricator burners fill first): powered fuel extractors fill their OWN small tank (full = production stalls), PIPELINE cables (ECableType::Pipe, fuel-only medium, "Pipeline throughput") move it, generators/fabricators burn from their OWN tank (fabricators need energy AND fuel). MINERALS ARE THE THIRD FLOW NETWORK (same band model, four levels: extractor/fabricator outputs (3) export into the Base (2), which exports into silos/connectors (1), which feed barracks/constructors (0) — the Base sits between producers and silos on purpose, so extractors still dump into it at full rate while its own trickle PREFERS flowing out over banking, keeping only what receivers cannot take; this replaced the old mineral-sink fiction): powered mineral extractors/fabricators fill their OWN buffer (full = stalls), CONVEYOR links (ECableType::Conveyor, "Conveyor throughput") move mineral stores, and only what sits in Mineral silos + the Base ("Mineral silo/base capacity" stores) is SPENDABLE (`m_minerals` per-team cache recomputed per tick; spending drains silos first, Base last). LINK TOPOLOGY: any two structures link directly IF BOTH have capacity in the link's medium (a silo takes no power cable, a wall takes nothing); links touching a CONNECTOR reach "Connector link range" (32) instead of "Cable max length" (18), and one Connector still carries exactly ONE medium across all its links — `cableAllowed(a, b, type[, ignoreCable])` enforces capacity + range + medium at aim, create AND retype time. SURFACE TENSION: pushing into bubbles scales push AND energy costs by (1 + tension × pressure) on BOTH sides ("Game/Shield/Surface tension", "Game/Enemies/Push tension", "Game/Structures/Pressure draw tension"); player/unit Accel defaults halved so steering loses the shoving match. CONSTRUCTION: placing is FREE — structures spawn as BLUEPRINTS (`Structure::blueprint`, gray tint via applyStructureTint, inert in every tick: no power/fields/income/flows, links pre-wire but carry nothing). HEALTH IS THE PROGRESS: a ghost spawns at 1 hp, materials HEAL it at cost/healthMax per hp (`investMaterials`), full health = built (blue health bar while blueprint; damage literally undoes construction). The SAME price repairs damaged built structures. CHEAT: "Game/Cheats/Free instant build" (Synced checkbox — the server's value rules all players) skips the blueprint phase — placements spawn built at full health, no materials. Materials arrive from (a) PLAYERS: a carried "Materials" inventory (HUD counter; "Game/Player/Materials max"; server-authoritative for ALL players — clients receive theirs via the snapshot game blob's materials byte) refilled near own-team Silos/Base (`takeStoredMinerals`) and invested into nearby BLUEPRINTS only (`fundNearbyBlueprint`, "Game/Construction" tweaks), dropped on death; or (b) the CONSTRUCTOR structure (Production hotbar, conveyor-fed mineral stock + powered, amber range ring in drawDebug) which funds the nearest own-team blueprint OR damaged structure in "Constructor range" (`fundNearbyBlueprint(..., includeRepairs)`); with NOTHING to build/repair it instead BOOSTS the nearest own-team barracks' spawn clock (`m_barracksBoost` map, +"Constructor boost rate" per constructor at "boost materials/s" + "boost energy/s"; Npc timers tick at 1+boost). Blueprint state mirrors via a GPl built flag + a GSt status-bits byte (bit0 powered, bit1 blueprint; caps GSt 79). NPC emitter drain (`addEmitterLoad`) is SHIELD-INDEPENDENT — collapsed units strain emitters like shielded ones. GRID PLACEMENT: every placement (extractors included — the node's position snaps too) aligns to a 2 m world grid (`StructureSystem::snapToGrid`; odd footprints center on a cell, even on a corner) and occupies `footprintCellsOf(type)`² cells (1x1 default; FuelTank/Bastion/Turret/Solar/Fabricator/MineralSilo 2x2; Barracks/Base 3x3; prefab Scale/HalfExtents = footprint half so boxes fill their cells. HEIGHTS vary via the baseshapes height variants — CubeHalf/CubeQuarter/Pillar2/Pillar3 are the 2 m cube with 0.5x/0.25x/2x/3x height, origin centered: Connector = Pillar3 pylon, Wall + Mineral silo = Pillar2, Battery = CubeHalf, Solar = CubeQuarter; the border wall renders a LOW CubeHalf but keeps a tall 10x20x10 collider (deliberate mismatch — invisible fence above the visible wall); `structureSpawnHeights` = each prefab's box HALF height so everything sits flush) — `cellsFree` refuses overlaps at aim (red ghost) AND at placeStructure (the MP seam), and FREE resource nodes reserve their extractor's future footprint (buildings can't block extractor placement; extracted nodes rely on the standing extractor's own cells). `StructureSystem::actorInFootprint` (a spatial query for GameUnitComponents — units AND player capsules) refuses a placement someone is STANDING in, checked at both seams as well; it is deliberately NOT folded into cellsFree, which doubles as the unit-spawn-point probe and must not refuse a cell just because units stand near it. The GHOST is the real box: `drawStructureGhost` wireframes the exact footprint square × the prefab's height (`spawnHeightOf` = box half height) plus the interior cell lines, green when clear and red when refused — every whitebox building IS a box, so the preview is exact, and the Wall line and Lance anchor draw the same ghost per segment. Fifteen placeables + link tools behind an RTS GRID HOTBAR — 12 slots on the GRID HOTKEYS QWER/ASDF/ZXCV (`c_gridKeys`, always visible in game mode, every slot also CLICKABLE — `slotAtScreenPos` → `activateSlot`, the ONE entry point for key and click; clicks over the hotbar never reach the world): every slot caption is a 3-5 char SHORTHAND, and structure slots take theirs from `c_structureShortNames` (indexed by EStructureType) — the SAME table the world tag over a building uses, so a slot and the thing it builds read identically (`buildItemLabel`; only the two link tools carry a label of their own, DISC/UPGR). The ROOT page (Select/Delete/Link modes) holds the two categories on Q/W (CMBT/PROD), the three LINK TOOLS armed directly on E (CONN), D (DISC) and F (UPGR) — `EPlayerMode::Link` + `m_linkTool`, no page of their own; same key or Esc/C/RMB exits, another switches tool in place — and DEL on X; a CATEGORY page holds its items in slot order (max 10) + CNCL on C + BACK on V. C / Esc / Tab = `cancelOneLevel` (a two-click step drops → the armed item disarms → the page or Delete mode returns to Select, one per press); V = straight back to Select. Q "Combat" (`c_combatItems`) [emitter, bastion (big anchor, hungry), lance (focused cone; TWO-CLICK placement — first click anchors, second aims the facing, fallback auto-faces away from the Base), wall (TWO-CLICK line of square segments every 2 m, per-segment cost, `c_wallMaxSegments` 16), turret (fires player-type projectiles — the "Projectile" NAME is the enemy contact-damage hook — at the nearest enemy in "Turret range", "Turret shot energy" per shot from its internal store; NpcSystem::tickTurrets), FOUR barracks variants (Barracks/Brute/Runner/Spitter — 3x3, conveyor-fed: each spawn pays the per-type "\<Type\> spawn materials" from the mineral store, up to "Barracks unit limit" per barracks; spawns the variant's ATTACK unit type at a free cell around the building (`freeSpawnPointAround`))], W "Production" — everything that is not a weapon, the old separate Distribution page folded in [generator, solar (free trickle, no fuel; flat CubeQuarter slab), extractor, fabricator (energy+fuel→minerals), constructor, connector, battery, fuel tank, mineral silo (the team's spendable bank)]. BARRACKS ROUTES: in Select mode with an own-team barracks selected, RIGHT-click on ground sets a waypoint (SHIFT+RMB appends, max `MaxRouteWaypoints` 6 — bounded by the 64B GqW request); spawned units march the waypoints (advance on touching each "Waypoint radius" circle; no combat mid-march) before their AI takes over. Server-validated (`queueRouteRequest`: own-team barracks only), mirrored via GRt (+ join replay), drawn as a green line chain + circles for the own team. GRID BOUNDS: `setPlacementBounds` covers the corridor interior so footprints can't clip the border walls; the same `cellsFree` doubles as the unit-spawn-point probe, E/D/F = the two-click LINK TOOLS on the root page (`updateLinkTool`, click one structure then the other): CONNECT creates a link with the medium inferred by `smartLinkTypeFor` (connector's carried medium > the source's output > the first medium both endpoints hold) and CHAINS — the second endpoint stays picked, so a run of clicks wires a line; Disconnect removes the pair's link; Upgrade retypes Basic→Heavy. The preview line is green/white/cyan for create/remove/upgrade, red when the pair refuses — then the item's grid key arms it, LMB places (no F confirm — F is a grid key), RMB/Esc disarms (fast chains: Q→Q→LMB places an emitter, W→E→LMB an extractor; `BuildItem` tables in Match.cpp, grid keys POLLED in updateModeSwitching; the engine's 1..9,0 hotbar routing is disabled in game mode). Cables: a pair holds ONE LINK PER MEDIUM (power + pipe + conveyor side by side; CONN skips media the pair already has, so clicking the same two again adds the next one); same pair + same type toggles off, same medium + other tier retypes in place. Cable draw: hue by type (yellow/cyan/orange/blue), brightness + pulse speed from `GameStructureLink::flowAvg` (a ~0.25 s EMA of the per-tick rate — raw transfers are bursty, a consumer drains then takes a full packet, which strobed the visuals; the connector gauges read the same smoothed value), moving pulse in the flow direction, FIXED height per type (power top, pipe middle, conveyor bottom). Clients run no flow sim, so they brighten cables from the endpoints' GSt-synced utilization instead. The BASES exist from game start only (`spawnBase`, `NumPlaceableStructures` guards them off the hotbar), one per team at the corridor ends: respawn anchor, always-on emitter (tickPower only touches emitter types), a passive trickle of Minerals at "Base income mult" (0.25) of an extractor, MINERAL storage only (no energy, no fuel — "Start fuel" was removed with the Base tank; power/fuel infrastructure must be built), conveyor-linkable, invulnerable (skipped in tickDamage — a dead Base would soft-lock the respawn). Cables/pipes are free two-click links ("Cable max length" tweak; STABLE structure ids — endpoints dying prunes the link; same pair + same type toggles off). Structure health: every structure has a `ForceQuery`; territory owned by ANY other team's bubble drains health; emitters shrink out over "Emitter shrink time" when starved, latch off until "Emitter restart charge", pay a pressure surcharge ("Emitter energy/s @ pressure 1") plus per-unit siege drain (addEmitterLoad: flat rate onto the NEAREST ACTIVE bubble; spitter shots deposit too). World labels: health bar over every damageable structure + a second bar for storage (energy yellow, fuel orange). UNIT SELECTION (Select mode, authority only — units simulate on the server): LMB DRAG (> 8 px) draws a box on the ground and on release selects every visible own-team unit whose screen position is inside (`updateUnitSelection`, `NpcSystem::queryVisibleUnits` + `worldToScreen`; SHIFT adds, a plain click clears); `m_selectedUnits` are owning EntityPtrs pruned on death, drawn with a green ring. EVERY RMB MOVE ORDER (ground, held re-aim, or building face) also goes to the selected units: `GameUnitComponent::orderMove` = a LOCKED target with `moveOrder` set (drops the route), auto-cleared within `waypointRadius` so the AI resumes; a DSL `setTarget` lock never clears. A FRESH order (the RMB press) can make the unit IGNORE the lane term for "Order flow blind" seconds so an old trail cannot pull it back the way it came — 0 by default now that a fresh order SEEDS its own lane (`FlowField::clearArea` exists but is unused). INTERACTION MODES (`GameMatch::EPlayerMode`; PLAYER COMBAT REMOVED — no shooting/melee, `EPlayerMode::None` is dead): neutral/default = SELECT (click to inspect: highlight ring + overhead info; RIGHT-click GROUND with an own barracks selected = waypoint route; RIGHT-click a building = a MOVE ORDER to it (linking is the CONN tool)), Q/W (root page) = Build with that category, E/D/F = the link tools (the hotbar page doubles as the mode indicator; V/Esc/C/Tab return to Select). Build mode ALSO click-selects (`updateSelectionClick`, shared with Select mode): with nothing armed every LMB inspects, and with a ghost armed a click the placement refuses (occupied cells — i.e. on a building) selects it instead of doing nothing, while any click that CAN place still places. RMB ALWAYS MOVES THE PLAYER, and cancelling rides along on the SAME press: (1) whatever step is pending is cancelled, one level at a time — a half-finished two-click flow (Lance aim, Wall line, link endpoint) drops first, the next RMB disarms the item (`disarmBuild`, same end state as the C slot) or leaves a link tool; (2) with an own barracks selected and the cursor on GROUND, the press is instead a route waypoint (SHIFT appends) — the ONLY consumer that sets `m_rmbConsumed`, because a rally point is a positive order, not a cancel, and pairing it with a move would drag the player to every waypoint; (3) everything else becomes a MOVE ORDER (`GamePlayer::setMoveTarget`, green destination ring): on GROUND the capsule walks to the clicked point and HOLDING RMB keeps re-aiming at the cursor; on a BUILDING the destination is the CLICKED ground point, pushed out to the nearest face of the inflated footprint (half footprint + 1.2 m) when it fell inside, so you walk to the side you clicked instead of grinding into the wall (one-shot, does not drag). Cancels deliberately do NOT consume — a cancel that also ate the movement read as a dropped input. A respawn drops a standing order (WASD movement no longer exists — the letter keys are grid hotkeys). Because RMB-hold now steers, the camera yaw drag moved to MIDDLE-mouse (Q/E unchanged). X (root page) = Delete toggle (click a structure to demolish, no refund, Base refused), C/Esc/Tab = one level back / return to Select. GqS/GqM requests and ALL of GamePlayer's combat code (tickCombat/queueShot/queueMelee/trackProjectile, the m_projectiles list, the "Game/Combat" tweaks) are deleted
* Units (`Game:Npc`, barracks-produced only): the SIM is `GameUnitComponent` in the entity pass (steering by the Nav flow fields — see Nav: nearest enemy by walking distance, routes around walls, own-team density spread; `GameMatch::feedNav` supplies obstacles = border ring + structure footprints and per-team sources = live non-invulnerable structures + player capsules + live units (kind 3, one arena-wide unit sweep per frame — UNIT-VS-UNIT COMBAT: non-ranged units melee enemy units in `attackRange + victim.bodyRadius` for attackDps, holding at that ring) — with the pre-Nav local search as the fallback where no field covers a unit: stuck-sidestep, auto-targeting = random pick among the 4 nearest enemy structures via spatial query + nearest-enemy-player fallback re-rolled on a jittered "Retarget interval", melee gnaw against the nearest enemy structure's meleeRadius ring, shield battery on player rules minus regen with permanent collapse, exposure damage, field push, ranged spitter stance raising `wantsFire`, death/void self-despawn). NpcSystem is PRODUCTION + SERVICING and holds NO unit state at all: a unit REPORTS what the game needs through `GameUnitComponent`'s static event queues (FireRequests for spitter shots, its own death carrying `sourceId`), and `service(structures)` drains them on the main thread; shield/health/team state needs NO publish step — it lives on the component and rides the entity snapshot's game blob. BARRACKS and TURRETS are per-entity too: `GameStructureComponent::update`'s machine block (discriminated by `machineKind`, stamped with the union variant) runs the spawn clock / target query ON the structure, pays minerals/energy from its own stores, claims the roster slot, and queues a SpawnRequest/TurretFireRequest — service() only performs the main-thread entity spawns (refunding a failed one). Production tuning lives on `GameStructureComponent::params` + the per-type `barracks.spawnCost` stamp (registered from StructureSystem, tweak names unchanged); NpcSystem keeps only the shot speeds. Roster counts are spawn/death EDGES (`++aliveUnits` at the decision, `--` on the death event; re-seeded by loadUnits); the overhead labels use `queryVisibleUnits` (a camera FRUSTUM query — an off-screen unit would only fail worldToScreen anyway; puppets label as players, own player skipped); the sole world-wide unit query left is save/load's, which must persist units the camera cannot see; `tickBarracks` spawns the variant's unit prefab (per-type stats AUTHORED in the .pre GameUnit block: Grunt baseline, Brute 180hp slow, Runner 30hp fast, Spitter ranged; barracks route copied onto the unit at spawn AND re-pushed to its live units when the route changes — the march index is kept and clamped, so appended routes continue, finished units march to the new tail), `tickTurrets` fires team-tagged projectile.pre at the nearest enemy-team unit. Projectiles are `GameProjectileComponent` (lifetime, field deflection, emitter sap, enemy-team contact damage — routed from World::handleContactEvent, first touch spends). There is NO published player list: units find enemy players (puppet GameUnitComponents, spatially registered like everything else) in the SAME queries as structures — the auto-target sweep ("Target search radius", default 50: LOCAL harassment, the barracks route does the long-distance delivery) takes the nearest enemy puppet as fallback, and the short melee probe chews any puppet in reach. The server stamps team onto client twins' puppets in the materials loop (claims never apply team — client-forgeable). PLAYER DAMAGE IS UNIFIED under `GameUnitComponent::damage()` — the ONE entry point for every victim: on a unit it CAS-subtracts health, on a PUPPET it atomically banks into `pendingDamage` (health is owner-computed, so a direct write would be stomped). Unit melee and enemy projectile contacts both just call damage() on whatever they hit — projectiles now hurt players through the same call (old known gap closed). The inbox drains main-thread via `takePendingDamage()`: GamePlayer::tickShieldAndHealth drains its OWN capsule, and the server's GDm flush (0.1 s) drains the client twins' inboxes directly — the component accumulates between flushes, so there is no clientId-keyed damage map. Carried MATERIALS live the same way: the twin's `materialsFrac` IS the server-authoritative store (no m_clientMaterials map; state dies with the capsule) — `m_clientPlayers` is the only per-client container left. `GamePlayer::applyDamage` ABSORBS into the shield battery first — "Game/Shield/Damage absorb (energy per hp)", overflow or collapsed hits health — and respects spawn grace
* Player (`Entities/Game/player.pre` = playerCapsule + `Component Force` shield): MOUSE-ONLY movement — RTS right-click move orders (see the RMB chain above — `setMoveTarget`/`hasMoveTarget`, "Game/Player/Move arrive radius") on the testbed's velocity-steering model, LShift sprint, Space jump; NO WASD (those keys are grid hotkeys — `tickMovement` ignores its camera-forward argument now). NO player combat (the combat code is fully deleted). Shield output shrinks under `getPressure()` and regens; `getAppliedForce()` pushes the capsule; health drains only when the shield is COLLAPSED and the query says enemy-owned, and REGENERATES at "Game/Player/Base heal/s" within "Base heal radius" of an own-team Base (`GameMatch::tickBaseHealing`, own player only — health is owner-computed, so every instance heals its own capsule against its local structure mirror, no sync); death teleport-respawns (prev/curr pose stomp per the teleport contract). All tunables under "Game/*" tweaks; HUD via `Globals::gameHud` (bars Health/Shield/Materials, counters Minerals/Fuel/Power, hotbar slot counts = affordable)